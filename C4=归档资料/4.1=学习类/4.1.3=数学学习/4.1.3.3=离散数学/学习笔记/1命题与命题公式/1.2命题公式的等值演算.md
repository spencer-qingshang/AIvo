# 1.2命题公式的等值演算

- 教材页码：24
- 来源：02324《离散数学》（2014 年版）

## 第 3 课：什么是等值

### 命题公式

用命题字母（如 `P`、`Q`）和联结词（如 `¬`、`∧`、`∨`）组成的式子，叫作命题公式。

例如：`¬P ∨ Q`，读作“非 P，或 Q”。

### 等值

两个命题公式在**所有可能的真值组合下，结果都相同**，就叫作等值，记作 `≡`。

最基础的例子：

`¬¬P ≡ P`

意思是“不是 P 的否定”等价于 P。比如“今天不是不下雨”就是“今天下雨”。

判断等值最可靠的办法是列真值表：只要每一行的结果都相同，就等值。

| P | ¬P | ¬¬P |
| --- | --- | --- |
| 真 | 假 | 真 |
| 假 | 真 | 假 |

可以看到，`¬¬P` 和 `P` 的结果完全一样，所以它们等值。

## 本节小练习（请你作答）

设 `P` 为假，`Q` 为真：

1. `¬¬P` 的真值是什么？
2. `P ∨ Q` 的真值是什么？
3. `¬(P ∨ Q)` 的真值是什么？

## 学习记录

- 状态：进行中
- 待完成：小练习 1 至 3
