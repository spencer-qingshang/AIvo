# 第五章数据查询：单项选择题

每题只有一个正确答案。完成后把答案发给我；批改时会统计成绩，并完整附上每道错题的原题、四个选项和解析。

以下题目统一使用：

```
student(student_id, name, gender, major, age)
course(course_id, course_name, credit)
score(student_id, course_id, grade)
```

## 一、SELECT与投影查询

1. SQL查询数据主要使用（A　）  
    A. `SELECT`　B. `UPDATE`　C. `DELETE`　D. `ALTER`
    
2. 指定查询数据来源的子句是（B　）  
    A. `WHERE`　B. `FROM`　C. `HAVING`　D. `LIMIT`
    
3. 查询学生姓名和专业，正确的是（A　）  
    A. `SELECT name, major FROM student;`  
    B. `SELECT student FROM name, major;`  
    C. `FROM student SELECT name, major;`  
    D. `SELECT name AND major IN student;`
    
4. 查询学生表全部列，正确的是（B　）  
    A. `SELECT ALL student;`　B. `SELECT * FROM student;`  
    C. `SHOW * student;`　D. `SELECT student.*;`
    
5. 投影查询主要是（A　）  
    A. 选择指定列　B. 筛选指定行　C. 对记录分组　D. 连接多张表
    
6. 将查询结果中的`name`列标题改为“姓名”，正确的是（　）  
    A. `SELECT name AS 姓名 FROM student;`  
    B. `SELECT name TO 姓名 FROM student;`  
    C. `SELECT name CHANGE 姓名 FROM student;`  
    D. `ALTER name 姓名 FROM student;`
    
7. 关于列别名，下列说法正确的是（　）  
    A. 必须使用`WHERE`指定　B. `AS`通常可以省略  
    C. 会修改原表列名　D. 只能用于数字列
    
8. 计算学生明年年龄且标题为`next_age`，正确的是（　）  
    A. `SELECT age + 1 AS next_age FROM student;`  
    B. `UPDATE age + 1 AS next_age FROM student;`  
    C. `SELECT age AS age + 1 FROM student;`  
    D. `SELECT next_age = age FROM student;`
    
9. 查询中的计算列通常（　）  
    A. 自动修改原表数据　B. 只影响查询结果  
    C. 删除原列　D. 必须写入新表
    
10. 查询学生表中出现过的专业并去重，正确的是（　）  
    A. `SELECT UNIQUE major FROM student;`  
    B. `SELECT DISTINCT major FROM student;`  
    C. `SELECT major DELETE REPEAT FROM student;`  
    D. `SELECT major GROUP DISTINCT student;`
    
11. `SELECT DISTINCT major, gender FROM student;`去重的依据是（　）  
    A. 只看`major`　B. 只看`gender`  
    C. `major`和`gender`的组合　D. 整张原表的全部列
    
12. 下列常用子句书写顺序正确的是（　）  
    A. `SELECT-FROM-WHERE-GROUP BY-HAVING-ORDER BY-LIMIT`  
    B. `FROM-SELECT-WHERE-LIMIT-GROUP BY`  
    C. `SELECT-WHERE-FROM-HAVING-GROUP BY`  
    D. `SELECT-FROM-ORDER BY-WHERE-LIMIT`
    
13. SELECT查询的逻辑处理顺序通常首先考虑（　）  
    A. `LIMIT`　B. `ORDER BY`　C. `FROM`　D. `SELECT`
    

## 二、WHERE选择查询

14. 选择查询主要是（　）  
    A. 选择满足条件的行　B. 只选择列名  
    C. 修改表结构　D. 删除重复列
    
15. 查询年龄不小于20岁的学生，正确的是（　）  
    A. `WHERE age => 20`　B. `WHERE age >= 20`  
    C. `WHERE age =< 20`　D. `WHERE age NOT <=> 20`
    
16. SQL中“不等于”可以表示为（　）  
    A. `<>`或`!=`　B. `==`或`!==`  
    C. `><`或`=!`　D. `NOT=`
    
17. 查询计算机专业且年龄大于20岁的学生，正确的是（　）  
    A. `WHERE major='计算机' AND age>20`  
    B. `WHERE major='计算机' OR age>20`  
    C. `WHERE major='计算机', age>20`  
    D. `WHERE major='计算机' HAVING age>20`
    
18. 逻辑运算符通常的优先级是（　）  
    A. `OR-AND-NOT`　B. `NOT-AND-OR`  
    C. `AND-OR-NOT`　D. `NOT-OR-AND`
    
19. 查询计算机或软件工程专业且年龄不小于20岁的学生，最清晰的是（　）  
    A. `WHERE major='计算机' OR major='软件工程' AND age>=20`  
    B. `WHERE (major='计算机' OR major='软件工程') AND age>=20`  
    C. `WHERE major=('计算机' OR '软件工程') AND age>=20`  
    D. `WHERE major IN '计算机','软件工程' OR age>=20`
    
20. `grade BETWEEN 80 AND 90`表示（　）  
    A. `grade>80 AND grade<90`  
    B. `grade>=80 AND grade<=90`  
    C. `grade<80 OR grade>90`  
    D. 只包括80，不包括90
    
21. 查询专业属于计算机、软件工程或人工智能，正确的是（　）  
    A. `WHERE major IN ('计算机','软件工程','人工智能')`  
    B. `WHERE major IS ('计算机','软件工程','人工智能')`  
    C. `WHERE major BETWEEN ('计算机','人工智能')`  
    D. `WHERE major = ALL ('计算机','软件工程')`
    
22. 查询专业不属于计算机和软件工程，正确的是（　）  
    A. `WHERE major OUT ('计算机','软件工程')`  
    B. `WHERE major NOT IN ('计算机','软件工程')`  
    C. `WHERE NOT major IS ('计算机','软件工程')`  
    D. `WHERE major <> ('计算机','软件工程')`
    
23. 查询专业为空值的记录，正确的是（　）  
    A. `WHERE major = NULL`　B. `WHERE major IS NULL`  
    C. `WHERE major == NULL`　D. `WHERE major EQUAL NULL`
    
24. 查询专业不为空的记录，正确的是（　）  
    A. `WHERE major != NULL`　B. `WHERE major <> NULL`  
    C. `WHERE major IS NOT NULL`　D. `WHERE major NOT = NULL`
    

## 三、LIKE与REGEXP

25. `LIKE`中的`%`表示（　）  
    A. 任意一个字符　B. 任意长度的任意字符  
    C. 一个数字　D. 一个空值
    
26. `LIKE`中的`_`表示（　）  
    A. 任意一个字符　B. 任意长度的字符  
    C. 任意一个数字　D. 字符串结尾
    
27. 查询姓名以“张”开头的学生，正确的是（　）  
    A. `WHERE name LIKE '%张'`　B. `WHERE name LIKE '张%'`  
    C. `WHERE name LIKE '%张%'`　D. `WHERE name = '张%'`
    
28. 查询姓名以“明”结尾的学生，正确的是（　）  
    A. `WHERE name LIKE '明%'`　B. `WHERE name LIKE '%明'`  
    C. `WHERE name LIKE '_明_'`　D. `WHERE name = '%明'`
    
29. `name LIKE '张__'`通常匹配（　）  
    A. 任意长度且包含张的姓名  
    B. 三个字符且第一个字符为张的姓名  
    C. 两个字符且最后一个字符为张的姓名  
    D. 只有“张”一个字符
    
30. 查询姓名以张或李开头，可使用（　）  
    A. `WHERE name REGEXP '^[张李]'`  
    B. `WHERE name REGEXP '[张李]$'`  
    C. `WHERE name LIKE '张|李%'`  
    D. `WHERE name REGEXP '^张李$'`
    
31. 正则表达式中的`^`和`$`分别表示（　）  
    A. 结尾和开头　B. 开头和结尾  
    C. 任意字符和数字　D. 重复和分组
    
32. `REGEXP '张|李'`中的`|`表示（　）  
    A. 并且　B. 或　C. 字符串结尾　D. 任意字符
    
33. 关于`LIKE`和`REGEXP`，正确的是（　）  
    A. `LIKE`适合简单匹配，`REGEXP`适合复杂模式匹配  
    B. 两者只能匹配数字  
    C. `LIKE`比正则表达式支持更多复杂规则  
    D. 两者的通配符完全相同
    

## 四、聚合函数与分组

34. 统计表中结果行总数，通常使用（　）  
    A. `SUM(*)`　B. `COUNT(*)`　C. `AVG(*)`　D. `TOTAL(*)`
    
35. 计算成绩总和使用（　）  
    A. `COUNT(grade)`　B. `SUM(grade)`  
    C. `AVG(grade)`　D. `MAX(grade)`
    
36. 计算平均成绩使用（　）  
    A. `MEAN(grade)`　B. `AVG(grade)`  
    C. `COUNT(grade)`　D. `MID(grade)`
    
37. 查询最高分和最低分，应使用（　）  
    A. `TOP()`和`BOTTOM()`　B. `MAX()`和`MIN()`  
    C. `HIGH()`和`LOW()`　D. `FIRST()`和`LAST()`
    
38. 关于`COUNT(*)`和`COUNT(grade)`，正确的是（　）  
    A. 二者一定完全相同  
    B. `COUNT(*)`统计行数，`COUNT(grade)`忽略`grade`为`NULL`的行  
    C. `COUNT(*)`忽略所有含`NULL`的行  
    D. `COUNT(grade)`统计所有列
    
39. 除`COUNT(*)`外，多数聚合函数对`NULL`的处理是（　）  
    A. 通常忽略`NULL`　B. 将`NULL`当作0  
    C. 遇到`NULL`必然报错　D. 删除原记录
    
40. 统计各专业人数，正确的是（　）  
    A. `SELECT major, COUNT(*) FROM student GROUP BY major;`  
    B. `SELECT major, COUNT(*) FROM student HAVING major;`  
    C. `SELECT COUNT(major) FROM student ORDER BY major;`  
    D. `SELECT major FROM student WHERE COUNT(*)>0;`
    
41. `GROUP BY`的主要作用是（　）  
    A. 对具有相同分组值的记录分组  
    B. 删除表中记录  
    C. 修改列标题  
    D. 限制返回行数
    
42. 按专业和性别的组合分组，正确的是（　）  
    A. `GROUP BY major AND gender`  
    B. `GROUP BY major, gender`  
    C. `GROUP major WITH gender`  
    D. `GROUP BY (major OR gender)`
    
43. 在严格分组规则下，`SELECT`中的非聚合列通常应（　）  
    A. 出现在`GROUP BY`中　B. 出现在`LIMIT`中  
    C. 只能是主键　D. 必须删除
    
44. 筛选平均成绩不低于80分的课程，正确的是（　）  
    A. `WHERE AVG(grade)>=80 GROUP BY course_id`  
    B. `GROUP BY course_id HAVING AVG(grade)>=80`  
    C. `HAVING grade>=80 GROUP BY course_id`  
    D. `ORDER BY AVG(grade)>=80`
    
45. `WHERE`和`HAVING`的主要区别是（　）  
    A. `WHERE`筛选分组前的行，`HAVING`筛选分组后的组  
    B. `WHERE`筛选组，`HAVING`筛选原始行  
    C. 二者只能用于排序  
    D. 二者没有区别
    
46. 下列语句的处理含义正确的是（　）
    

```
SELECT course_id, AVG(grade)
FROM score
WHERE grade >= 60
GROUP BY course_id
HAVING AVG(grade) >= 80;
```

A. 先保留及格记录，再分组并筛选平均分不低于80的组  
B. 先筛选平均分，再删除不及格课程  
C. 查询每名学生的最高成绩  
D. 修改低于80分的成绩

## 五、排序与LIMIT

47. 查询结果默认按年龄升序排列，正确的是（　）  
    A. `ORDER BY age ASC`　B. `ORDER age UP`  
    C. `GROUP BY age ASC`　D. `SORT age`
    
48. `ORDER BY grade DESC`表示（　）  
    A. 按成绩升序　B. 按成绩降序  
    C. 按成绩分组　D. 删除重复成绩
    
49. `ORDER BY`省略排序方向时，通常默认为（　）  
    A. `DESC`　B. `ASC`　C. 随机　D. 按主键降序
    
50. `ORDER BY major ASC, age DESC`表示（　）  
    A. 只按年龄降序  
    B. 先按专业升序，专业相同时按年龄降序  
    C. 先按年龄降序，再按专业升序  
    D. 同时对所有列降序
    
51. 返回查询结果前5行，正确的是（　）  
    A. `LIMIT 5`　B. `TOP 5`　C. `FIRST 5`　D. `ROW 5`
    
52. `LIMIT 5,3`表示（　）  
    A. 返回第3至第5行  
    B. 跳过前5行，再返回3行  
    C. 跳过前3行，再返回5行  
    D. 只返回第5行
    
53. `LIMIT`中的偏移量通常从（　）开始计数。  
    A. 0　B. 1　C. -1　D. 10
    
54. `LIMIT 3 OFFSET 5`等价于（　）  
    A. `LIMIT 3,5`　B. `LIMIT 5,3`  
    C. `LIMIT 8`　D. `LIMIT 5 OFFSET 3`
    
55. 查询成绩最高的3条记录，正确的是（　）  
    A. `SELECT * FROM score LIMIT 3 ORDER BY grade DESC;`  
    B. `SELECT * FROM score ORDER BY grade DESC LIMIT 3;`  
    C. `SELECT * FROM score GROUP BY grade LIMIT 3;`  
    D. `SELECT TOP 3 * FROM score;`
    

## 六、连接查询

56. 单表查询与多表查询的主要区别是（　）  
    A. 数据是否来自多张表　B. 是否必须使用`SELECT`  
    C. 是否允许使用条件　D. 是否能够返回结果
    
57. 交叉连接产生（　）  
    A. 差集　B. 笛卡尔积　C. 交集　D. 分组结果
    
58. 学生表有4行、课程表有3行，交叉连接结果有（　）  
    A. 7行　B. 12行　C. 4行　D. 3行
    
59. 内连接返回（　）  
    A. 两表中满足连接条件的记录  
    B. 左表全部记录，无论是否匹配  
    C. 两表全部未匹配记录  
    D. 两表的所有组合
    
60. 下列内连接语法正确的是（　）  
    A. `FROM student s JOIN score sc ON s.student_id=sc.student_id`  
    B. `FROM student s JOIN score sc WHERE ON s.student_id=sc.student_id`  
    C. `FROM student s CONNECT score sc BY student_id`  
    D. `FROM student s INNER score sc WITH student_id`
    
61. `INNER JOIN`中的`INNER`通常（　）  
    A. 可以省略　B. 必须改写为`OUTER`  
    C. 只能用于单表　D. 表示左外连接
    
62. 连接条件使用等号的连接称为（　）  
    A. 等值连接　B. 非等值连接　C. 交叉连接　D. 联合查询
    
63. 下列属于非等值连接条件的是（　）  
    A. `ON a.id=b.id`  
    B. `ON score.grade BETWEEN level.low_score AND level.high_score`  
    C. `ON a.id=a.id`  
    D. `ON a.name=b.name`
    
64. 自然连接通常（　）  
    A. 自动按同名列连接，并只保留一份同名列  
    B. 必须明确写出`ON`条件  
    C. 删除所有同名列  
    D. 必然产生笛卡尔积
    
65. 两个关系没有同名列时，自然连接通常相当于（　）  
    A. 差运算　B. 笛卡尔积　C. 左连接　D. 子查询
    
66. 左外连接返回（　）  
    A. 仅右表全部记录  
    B. 左表全部记录及右表中匹配的记录  
    C. 仅两表匹配记录  
    D. 仅左表未匹配记录
    
67. 左连接中左表记录没有匹配时，右表对应列通常显示（　）  
    A. 0　B. 空字符串　C. `NULL`　D. 左表原值
    
68. 查询没有选课的学生，较合适的是（　）  
    A. 左连接后判断右表连接列`IS NULL`  
    B. 内连接后判断成绩大于0  
    C. 交叉连接后排序  
    D. 右连接后删除学生表
    
69. 右外连接返回（　）  
    A. 右表全部记录及左表中匹配的记录  
    B. 左表全部记录  
    C. 仅两表未匹配记录  
    D. 仅两表匹配记录
    
70. 全外连接返回（　）  
    A. 仅左表记录　B. 仅右表记录  
    C. 两表匹配及双方未匹配的记录　D. 仅匹配记录
    
71. 关于MySQL的全外连接，正确的是（　）  
    A. MySQL可直接使用`FULL OUTER JOIN`  
    B. MySQL通常用左、右连接配合`UNION`模拟  
    C. MySQL只能使用自然连接  
    D. MySQL用`CROSS JOIN`实现全外连接
    
72. 自连接是指（　）  
    A. 一张表以不同别名与自身连接  
    B. 不写任何表名  
    C. 把数据库连接到自身  
    D. 只查询主键
    
73. 自连接中使用表别名的主要目的是（　）  
    A. 区分同一张表扮演的不同角色  
    B. 修改永久表名  
    C. 删除重复记录  
    D. 创建新数据库
    
74. 对内连接而言，连接条件写在`ON`中与旧式写在`WHERE`中，结果通常（　）  
    A. 等价　B. 必然完全不同  
    C. 都会成为外连接　D. 都会报错
    
75. 通常推荐使用`JOIN ... ON`是因为（　）  
    A. 能清楚区分连接条件和筛选条件  
    B. 不需要连接条件  
    C. 会自动修改原表  
    D. 只能返回一行
    
76. 对外连接而言，将右表筛选条件从`ON`移到`WHERE`中可能（　）  
    A. 过滤掉右表不匹配产生的`NULL`行  
    B. 自动变成交叉连接  
    C. 删除右表结构  
    D. 完全不可能改变结果
    

## 七、子查询

77. 子查询是（　）  
    A. 嵌套在另一条SQL语句中的查询  
    B. 删除子表的语句  
    C. 只能查询一列的语句  
    D. 修改表结构的查询
    
78. `IN`子查询适合用于（　）  
    A. 判断某值是否属于子查询返回的一组值  
    B. 修改表结构  
    C. 只判断子查询列名  
    D. 排序查询结果
    
79. 查询选修C01课程的学生，正确的是（　）
    

A.

```
SELECT * FROM student
WHERE student_id IN (
  SELECT student_id FROM score WHERE course_id='C01'
);
```

B. `SELECT * FROM student WHERE student_id = score;`  
C. `SELECT * FROM student GROUP BY course_id='C01';`  
D. `SELECT * FROM student UNION course_id='C01';`

80. 使用`=`、`>`等普通比较符与子查询比较时，子查询通常应返回（　）  
    A. 单个值　B. 任意多列多行　C. 整个数据库　D. 表结构
    
81. 查询年龄大于平均年龄的学生，正确的是（　）
    

A.

```
SELECT * FROM student
WHERE age > (SELECT AVG(age) FROM student);
```

B. `SELECT * FROM student WHERE age > AVG(age);`  
C. `SELECT AVG(age) FROM student WHERE student_id;`  
D. `SELECT * FROM student GROUP BY age > AVG(age);`

82. `> ANY(子查询)`表示（　）  
    A. 大于子查询返回值中的至少一个  
    B. 大于子查询返回的每一个值  
    C. 等于所有返回值  
    D. 子查询不得返回值
    
83. `> ALL(子查询)`表示（　）  
    A. 大于其中至少一个值　B. 大于所有返回值  
    C. 小于所有返回值　D. 等于其中一个值
    
84. `EXISTS`主要用于（　）  
    A. 检查子查询是否至少返回一行  
    B. 比较两个列名是否相同  
    C. 返回子查询中的第一列  
    D. 删除重复结果
    
85. `EXISTS`子查询中的`SELECT 1`表示（　）  
    A. 返回的数字1将作为最终查询结果  
    B. `EXISTS`只关心是否存在结果行，选择什么常量通常不重要  
    C. 子查询只能返回数字1  
    D. 只查询主键为1的记录
    
86. 查询至少选修过一门课程的学生，较合适的是（　）  
    A. `WHERE EXISTS (...)`　B. `WHERE NOT EXISTS (...)`  
    C. `WHERE grade IS NULL`　D. `CROSS JOIN`
    
87. 查询没有选修任何课程的学生，较合适的是（　）  
    A. `WHERE EXISTS (...)`　B. `WHERE NOT EXISTS (...)`  
    C. `WHERE student_id = NULL`　D. `INNER JOIN`后计数
    
88. 子查询引用外层查询当前行的列，这种查询称为（　）  
    A. 相关子查询　B. 联合查询　C. 交叉查询　D. 投影查询
    
89. 关于`NOT IN`，需要特别注意的是（　）  
    A. 子查询中存在`NULL`时可能产生意外结果  
    B. 它不能用于子查询  
    C. 它与`IN`完全相同  
    D. 它会修改原表
    

## 八、UNION联合查询

90. 联合查询的主要作用是（　）  
    A. 将多个查询结果纵向合并  
    B. 将多张表横向连接  
    C. 修改多个表的结构  
    D. 删除重复列
    
91. 使用`UNION`的各个查询通常必须（　）  
    A. 返回相同数量的列，且对应类型兼容  
    B. 使用相同表名  
    C. 返回相同数量的行  
    D. 具有相同主键
    
92. `UNION`中各列按（　）对应。  
    A. 位置　B. 列名字母顺序　C. 主键值　D. 表名
    
93. 联合结果的列标题通常由（　）决定。  
    A. 第一个查询　B. 最后一个查询  
    C. 行数最多的查询　D. 数据库名称
    
94. `UNION`与`UNION ALL`的区别是（　）  
    A. `UNION`去重，`UNION ALL`保留重复行  
    B. `UNION ALL`去重，`UNION`保留重复行  
    C. `UNION`横向连接，`UNION ALL`纵向连接  
    D. 二者完全相同
    
95. 不需要去重时，通常效率更高的是（　）  
    A. `UNION ALL`　B. `UNION`　C. `NATURAL JOIN`　D. `EXISTS`
    
96. 对整个联合结果排序时，`ORDER BY`通常放在（　）  
    A. 最后一个查询之后　B. 第一个`SELECT`之前  
    C. 每个`FROM`之前　D. `UNION`关键字中间
    

## 九、综合应用

97. 查询各课程平均成绩，并按平均成绩降序排列，正确的是（　）

A.

```
SELECT course_id, AVG(grade) AS avg_grade
FROM score
GROUP BY course_id
ORDER BY avg_grade DESC;
```

B. `SELECT course_id, grade FROM score ORDER BY AVG(grade);`  
C. `SELECT AVG(grade) FROM score WHERE course_id;`  
D. `SELECT course_id FROM score HAVING grade DESC;`

98. 查询平均成绩最高的3门课程，正确的是（　）

A.

```
SELECT course_id, AVG(grade) AS avg_grade
FROM score
GROUP BY course_id
ORDER BY avg_grade DESC
LIMIT 3;
```

B.

```
SELECT course_id, AVG(grade)
FROM score LIMIT 3 GROUP BY course_id;
```

C. `SELECT TOP 3 course_id FROM score;`  
D. `SELECT course_id FROM score WHERE AVG(grade)>3;`

99. 查询学生姓名、课程名和成绩，通常需要（　）  
    A. 连接`student`、`score`和`course`三张表  
    B. 只查询`student`表  
    C. 只查询`course`表  
    D. 使用`DROP TABLE`
    
100. 查询选课人数不少于3人的课程，分组后应使用（　）  
    A. `WHERE COUNT(*)>=3`　B. `HAVING COUNT(*)>=3`  
    C. `LIMIT COUNT(*)>=3`　D. `ORDER BY COUNT(*)>=3`