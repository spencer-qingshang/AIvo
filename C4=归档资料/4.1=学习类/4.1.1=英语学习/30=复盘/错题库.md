# 错题库

> 自动汇总所有 `type: error` 的笔记，按错误原因分类。

## 语法错误
```dataview
LIST FROM "C4=归档资料/4.1=学习类/4.1.1=英语学习"
WHERE type = "error" AND contains(reason, "语法")
```

## 拼写错误
```dataview
LIST FROM "C4=归档资料/4.1=学习类/4.1.1=英语学习"
WHERE type = "error" AND contains(reason, "拼写")
```

## 词汇错误
```dataview
LIST FROM "C4=归档资料/4.1=学习类/4.1.1=英语学习"
WHERE type = "error" AND contains(reason, "词汇")
```
