# 第四章表数据操作——单项选择题

（每小题有4个备选项，只有一个符合题目要求。完成后把答案发给我；批改时会完整附上每道错题的原题、四个选项和解析。）

## 一、基本概念

1. 向表中增加记录通常使用（A　）

A. `INSERT`  
B. `UPDATE`  
C. `DELETE`  
D. `DROP`

2. 修改表中已有记录通常使用（B　）

A. `ALTER TABLE`  
B. `UPDATE`  
C. `INSERT`  
D. `TRUNCATE`

3. 删除表中指定记录通常使用（C　）

A. `DROP TABLE`  
B. `ALTER TABLE`  
C. `DELETE`  
D. `CREATE TABLE`

4. 下列命令中，主要用于操作表中数据而非表结构的是（D　）

A. `CREATE TABLE`  
B. `ALTER TABLE`  
C. `DROP TABLE`  
D. `INSERT`

5. 关于 `ALTER TABLE` 和 `UPDATE`，正确的是（C　）

A. 二者都用于修改记录  
B. 前者修改表结构，后者修改表中数据  
C. 前者修改数据，后者修改表结构  
D. 二者都用于删除表

6. 下列属于数据操纵语言DML命令的是（A　）

A. `INSERT、UPDATE、DELETE`  
B. `CREATE、ALTER、DROP`  
C. `GRANT、REVOKE、COMMIT`  
D. `USE、SHOW、DESC`

---

## 二、插入一条记录

使用下列表结构回答有关问题：

```
CREATE TABLE student (
    student_id CHAR(4) PRIMARY KEY,
    name VARCHAR(20) NOT NULL,
    gender CHAR(1) DEFAULT '男',
    age TINYINT UNSIGNED,
    major VARCHAR(30)
);
```

7. 为 `student` 表的所有列插入一条记录，正确的是（B　）

A.

```
INSERT student
('S001', '张三', '男', 20, '计算机');
```

B.

```
INSERT INTO student
VALUES ('S001', '张三', '男', 20, '计算机');
```

C.

```
INSERT FROM student
VALUES ('S001', '张三', '男', 20, '计算机');
```

D.

```
UPDATE student
VALUES ('S001', '张三', '男', 20, '计算机');
```

8. 省略插入语句中的列名列表时，`VALUES` 中值的顺序应当（C　）

A. 任意排列  
B. 按主键顺序排列  
C. 与表中列的定义顺序对应  
D. 按值的数据类型排列

9. 省略列名为表的所有列插入数据时，通常要求（A　）

A. 值的数量与表中列数一致  
B. 只能为字符列赋值  
C. 所有值都必须为数字  
D. 主键列必须放在最后

10. 为指定列插入数据的正确基本格式是（A　）

A.

```
INSERT INTO 表名 (列名列表)
VALUES (值列表);
```

B.

```
INSERT VALUES 表名 (列名列表);
```

C.

```
UPDATE 表名 VALUES (值列表);
```

D.

```
INSERT FROM 表名 SET (值列表);
```

11. 下列指定列插入语句正确的是（A　）

A.

```
INSERT INTO student (student_id, name, age)
VALUES ('S002', '李四', 19);
```

B.

```
INSERT INTO student
VALUES (student_id, name, age)
('S002', '李四', 19);
```

C.

```
INSERT student (student_id, name, age)
FROM ('S002', '李四', 19);
```

D.

```
UPDATE student (student_id, name, age)
VALUES ('S002', '李四', 19);
```

12. 执行第11题A选项的语句后，未指定的 `gender` 列通常取（B　）

A. 一定报错  
B. 默认值“男”  
C. 数值0  
D. 学生姓名

13. 如果插入时没有指定某列，该列允许为空且没有默认值，则该列通常取（A　）

A. `NULL`  
B. 空字符串  
C. 数字0  
D. 列名本身

14. 如果某列定义为 `NOT NULL` 且没有默认值，插入时又没有为它提供值，通常会（C　）

A. 自动填入空字符串  
B. 自动删除该列  
C. 插入失败  
D. 自动填入数字0

15. 指定列插入时，列的排列顺序（B　）

A. 必须与表结构中的顺序完全一致  
B. 可以改变，但值必须与指定列的顺序对应  
C. 必须按字母顺序排列  
D. 必须把主键放在最后

16. 下列语句中，列和值对应正确的是（A　）

A.

```
INSERT INTO student (name, age, student_id)
VALUES ('王五', 21, 'S003');
```

B.

```
INSERT INTO student (name, age, student_id)
VALUES ('S003', '王五', 21);
```

C.

```
INSERT INTO student (name, age)
VALUES ('王五');
```

D.

```
INSERT INTO student (student_id)
VALUES ('S003', '王五');
```

17. SQL中的 `NULL` 表示（C　）

A. 数字0  
B. 长度为0的字符串  
C. 没有值或未知  
D. 字符串`NULL`

18. 下列能向 `age` 列插入空值的是（假设该列允许为空）（B　）

A. `age = 'NULL'`  
B. `age = NULL`  
C. `age = ' '`  
D. `age = 0`

19. 关于 `NULL` 和 `'NULL'`，正确的是（B　）

A. 二者完全相同  
B. 前者是空值，后者是字符串  
C. 前者是字符串，后者是空值  
D. 二者都表示数字0

20. 插入时希望某列使用其默认值，可以使用（A　）

A. `DEFAULT`  
B. `PRIMARY`  
C. `CHECK`  
D. `UNSIGNED`

21. 下列插入语句正确使用默认值的是（A　）

```
-- gender的默认值为“男”
```

A.

```
INSERT INTO student
(student_id, name, gender)
VALUES ('S004', '赵六', DEFAULT);
```

B.

```
INSERT INTO student
(student_id, name, gender)
VALUES ('S004', '赵六', PRIMARY);
```

C.

```
INSERT INTO student
(student_id, name, gender)
VALUES ('S004', '赵六', UNSIGNED);
```

D.

```
INSERT INTO student
(student_id, name, gender)
VALUES ('S004', '赵六', CHECK);
```

22. 若表中已经存在主键为 `S001` 的记录，再用普通 `INSERT` 插入相同主键，通常会（B　）

A. 自动修改旧记录  
B. 产生主键冲突并插入失败  
C. 自动删除整张表  
D. 允许生成两条完全相同的主键记录

23. 执行 `INSERT` 时，插入的数据必须（C　）

A. 只满足主键约束  
B. 只满足非空约束  
C. 满足表中适用的各项完整性约束  
D. 不需要满足任何约束

---

## 三、一次插入多条记录

24. 一次插入多条记录时，各条记录之间通常使用（B　）

A. 句号  
B. 逗号  
C. 冒号  
D. `AND`

25. 一次插入多条记录的正确写法是（A　）

A.

```
INSERT INTO student (student_id, name)
VALUES ('S005', '钱一'),
       ('S006', '孙二');
```

B.

```
INSERT INTO student (student_id, name)
VALUES ('S005', '钱一')
VALUES ('S006', '孙二');
```

C.

```
INSERT INTO student (student_id, name)
('S005', '钱一'), ('S006', '孙二');
```

D.

```
UPDATE student (student_id, name)
VALUES ('S005', '钱一'), ('S006', '孙二');
```

26. 在一条多行插入语句中，关键字 `VALUES` 通常写（A　）

A. 一次  
B. 两次  
C. 每条记录前写一次  
D. 不需要写

27. 多行插入时，每组圆括号通常表示（C　）

A. 一列  
B. 一个数据库  
C. 一条记录  
D. 一个约束

28. 下列多行插入语句错误的原因是（B　）

```
INSERT INTO student (student_id, name)
VALUES ('S007', '周三'),
VALUES ('S008', '吴四');
```

A. 表名不能是`student`  
B. `VALUES`被重复书写  
C. 不能一次插入两条记录  
D. 字符串不能使用单引号

29. 一次插入多条记录时，每组值通常都应（A　）

A. 与指定列的数量和顺序对应  
B. 具有不同的列数  
C. 省略主键  
D. 只包含一个值

---

## 四、REPLACE语句

30. `REPLACE` 语句属于（A　）

A. MySQL扩展的插入或替换语句  
B. 修改表结构的标准命令  
C. 查看表结构的命令  
D. 删除数据库的命令

31. 使用 `REPLACE` 插入数据时，如果不存在主键或唯一键冲突，则通常（B　）

A. 删除整张表  
B. 插入一条新记录  
C. 修改表结构  
D. 拒绝执行

32. 使用 `REPLACE` 时，如果发生主键或唯一键冲突，通常会（B　）

A. 保留旧记录并忽略新记录  
B. 先删除旧记录，再插入新记录  
C. 删除整个数据库  
D. 只修改语句中提供的列，其他列必然保持不变

33. 下列 `REPLACE` 语句写法正确的是（A　）

A.

```
REPLACE INTO student (student_id, name)
VALUES ('S001', '张三');
```

B.

```
REPLACE student FROM
('S001', '张三');
```

C.

```
UPDATE REPLACE student
VALUES ('S001', '张三');
```

D.

```
REPLACE TABLE student
SET ('S001', '张三');
```

34. 关于 `REPLACE` 和 `UPDATE`，正确的是（B　）

A. 二者在任何情况下都完全相同  
B. `UPDATE`修改已有记录，`REPLACE`发生键冲突时通常删除旧记录再插入  
C. `REPLACE`只能修改表结构  
D. `UPDATE`只能增加记录

35. 原记录包含`student_id、name、age、major`。执行发生主键冲突的下列语句：

```
REPLACE INTO student (student_id, name)
VALUES ('S001', '张三');
```

未提供的 `age` 和 `major` 通常会（B　）

A. 必然保留旧值  
B. 按新记录重新取默认值或`NULL`  
C. 自动成为主键  
D. 自动复制`name`的值

36. 如果只想修改已有记录的一个字段，同时保留其他字段原值，通常更适合使用（A　）

A. `UPDATE`  
B. `REPLACE`  
C. `DROP TABLE`  
D. `TRUNCATE TABLE`

37. `REPLACE` 判断记录冲突的主要依据是（B　）

A. 普通列是否为空  
B. 主键或唯一键  
C. 表中记录总数  
D. 列的数据类型数量

---

## 五、插入查询结果

38. 将查询结果插入目标表，通常使用（A　）

A. `INSERT ... SELECT`  
B. `UPDATE ... DROP`  
C. `DELETE ... SELECT`  
D. `TRUNCATE ... WHERE`

39. 下列插入查询结果的基本格式正确的是（A　）

A.

```
INSERT INTO 目标表 (目标列)
SELECT 查询列
FROM 源表;
```

B.

```
INSERT INTO 目标表 (目标列)
VALUES SELECT 查询列 FROM 源表;
```

C.

```
SELECT 查询列
INSERT INTO 目标表 FROM 源表;
```

D.

```
UPDATE 目标表
SELECT 查询列 FROM 源表;
```

40. `INSERT ... SELECT` 语句中，在 `INSERT` 与 `SELECT` 之间（B　）

A. 必须使用`VALUES`  
B. 不使用`VALUES`  
C. 必须使用`UPDATE`  
D. 必须使用`DELETE`

41. 执行 `INSERT ... SELECT` 时，目标列数和查询结果列数应当（B　）

A. 完全无关  
B. 通常一致  
C. 目标列数必须更多  
D. 查询结果列数必须更多

42. 插入查询结果时，目标列和查询结果列还应满足（A　）

A. 数据类型兼容  
B. 列名必须完全相同  
C. 两张表必须同名  
D. 两张表的记录数必须相同

43. 下列将计算机专业学生复制到 `computer_student` 表的语句正确的是（A　）

A.

```
INSERT INTO computer_student (student_id, name)
SELECT student_id, name
FROM student
WHERE major = '计算机';
```

B.

```
INSERT INTO computer_student (student_id, name)
VALUES
SELECT student_id, name FROM student;
```

C.

```
UPDATE computer_student
FROM student WHERE major = '计算机';
```

D.

```
INSERT SELECT
computer_student FROM student;
```

44. 下列插入查询结果语句可能因列数不一致而失败的是（D　）

A.

```
INSERT INTO target_table (id, name)
SELECT id, name FROM source_table;
```

B.

```
INSERT INTO target_table (id, name)
SELECT id, name, age FROM source_table;
```

C.

```
INSERT INTO target_table (id)
SELECT id FROM source_table;
```

D.

```
INSERT INTO target_table (name)
SELECT name FROM source_table;
```

45. `INSERT ... SELECT` 中的 `WHERE` 子句用于（A　）

A. 筛选需要插入的源数据  
B. 修改目标表结构  
C. 删除目标表  
D. 指定数据库字符集

---

## 六、UPDATE修改数据

46. `UPDATE` 语句的基本格式是（A　）

A.

```
UPDATE 表名
SET 列名 = 新值
WHERE 条件;
```

B.

```
UPDATE FROM 表名
VALUES (新值);
```

C.

```
ALTER TABLE 表名
SET 列名 = 新值;
```

D.

```
MODIFY 表名
WHERE 列名 = 新值;
```

47. `UPDATE` 语句中的 `SET` 用于（B　）

A. 确定修改哪些行  
B. 指定列的新值  
C. 删除表结构  
D. 创建数据库

48. `UPDATE` 语句中的 `WHERE` 用于（A　）

A. 指定要修改的记录  
B. 指定表的存储引擎  
C. 指定新建列的数据类型  
D. 指定数据库名称

49. 将学号为 `S001` 的学生年龄修改为21，正确的是（A　）

A.

```
UPDATE student
SET age = 21
WHERE student_id = 'S001';
```

B.

```
UPDATE student
WHERE student_id = 'S001'
VALUES age = 21;
```

C.

```
ALTER TABLE student
SET age = 21;
```

D.

```
INSERT INTO student
SET age = 21
WHERE student_id = 'S001';
```

50. 使用一条 `UPDATE` 语句修改多个列时，各赋值之间使用（A　）

A. 逗号  
B. `AND`  
C. `OR`  
D. 分号

51. 同时修改 `S001` 学生的年龄和专业，正确的是（A　）

A.

```
UPDATE student
SET age = 21, major = '计算机'
WHERE student_id = 'S001';
```

B.

```
UPDATE student
SET age = 21 AND major = '计算机'
WHERE student_id = 'S001';
```

C.

```
UPDATE student
VALUES age = 21, major = '计算机'
WHERE student_id = 'S001';
```

D.

```
ALTER TABLE student
SET age = 21, major = '计算机';
```

52. 将计算机专业且年龄小于20岁的学生年龄改为20，正确的是（A　）

A.

```
UPDATE student
SET age = 20
WHERE major = '计算机' AND age < 20;
```

B.

```
UPDATE student
WHERE major = '计算机'
SET age < 20 = 20;
```

C.

```
UPDATE student
SET major = '计算机' AND age = 20;
```

D.

```
INSERT student
SET age = 20
WHERE major = '计算机';
```

53. 将计算机专业学生的年龄在原值基础上增加1，正确的是（A　）

A.

```
UPDATE student
SET age = age + 1
WHERE major = '计算机';
```

B.

```
UPDATE student
SET age + 1
WHERE major = '计算机';
```

C.

```
INSERT INTO student
SET age = age + 1;
```

D.

```
ALTER TABLE student
ADD age = 1;
```

54. 将图书类商品价格提高10%，正确的是（A　）

A.

```
UPDATE product
SET price = price * 1.10
WHERE category = '图书';
```

B.

```
UPDATE product
SET price = 1.10
WHERE category = '图书';
```

C.

```
ALTER TABLE product
SET price = price + 10;
```

D.

```
UPDATE product
WHERE price = price * 1.10;
```

55. 执行下列语句会产生什么结果？（B　）

```
UPDATE student
SET major = '计算机';
```

A. 只修改第一条记录  
B. 修改所有记录  
C. 删除所有记录  
D. 因没有`WHERE`而必然语法错误

56. 关于 `UPDATE` 中的 `WHERE`，正确的是（B　）

A. 省略`WHERE`只修改一条记录  
B. 省略`WHERE`通常修改全部记录  
C. `WHERE`用于指定新值  
D. `WHERE`只能与`DELETE`一起使用

57. 执行大范围 `UPDATE` 前，较稳妥的做法是（A　）

A. 先使用相同的`WHERE`条件执行`SELECT`检查目标记录  
B. 先删除表结构  
C. 先删除数据库  
D. 将所有字段改为`NULL`

58. 下列语句中，`WHERE` 和 `SET` 的作用匹配正确的是（B　）

A. `WHERE`决定改什么值，`SET`决定改哪些行  
B. `SET`决定改什么值，`WHERE`决定改哪些行  
C. 二者都只用于创建表  
D. 二者作用完全相同

59. 修改全部记录和修改指定记录的主要区别在于（B　）

A. 是否使用`SET`  
B. 是否使用`WHERE`限定范围  
C. 是否使用`INTO`  
D. 是否使用`VALUES`

---

## 七、DELETE删除数据

60. 删除表中记录的基本语句是（A　）

A.

```
DELETE FROM 表名
WHERE 条件;
```

B.

```
DELETE TABLE 表名
WHERE 条件;
```

C.

```
DROP FROM 表名
WHERE 条件;
```

D.

```
REMOVE ROW 表名;
```

61. 删除学号为 `S001` 的学生记录，正确的是（A　）

A.

```
DELETE FROM student
WHERE student_id = 'S001';
```

B.

```
DELETE student_id = 'S001'
FROM student;
```

C.

```
DROP TABLE student
WHERE student_id = 'S001';
```

D.

```
ALTER TABLE student
DELETE student_id = 'S001';
```

62. 删除所有年龄小于18岁的学生，正确的是（A　）

A.

```
DELETE FROM student
WHERE age < 18;
```

B.

```
DELETE student
SET age < 18;
```

C.

```
DROP FROM student
WHERE age < 18;
```

D.

```
TRUNCATE TABLE student
WHERE age < 18;
```

63. 执行下列语句会（A　）

```
DELETE FROM student;
```

A. 删除表中全部记录，但保留表结构  
B. 只删除第一条记录  
C. 删除表结构  
D. 删除数据库

64. 使用 `DELETE` 删除全部记录时，应当（B　）

A. 省略`FROM`  
B. 省略`WHERE`  
C. 省略表名  
D. 使用`DROP DATABASE`

65. 下列关于 `DELETE` 的说法正确的是（B　）

A. 只能删除全部记录  
B. 可以通过`WHERE`删除指定记录，也可以删除全部记录  
C. 会永久删除表结构  
D. 不能使用条件

66. 执行下列语句前，最需要确认的是（A　）

```
DELETE FROM student;
```

A. 是否确实要删除全部记录  
B. 是否要修改表名  
C. 是否要增加一列  
D. 是否要创建数据库

67. 删除计算机专业且年龄大于25岁的学生，正确的是（A　）

A.

```
DELETE FROM student
WHERE major = '计算机' AND age > 25;
```

B.

```
DELETE student
SET major = '计算机', age > 25;
```

C.

```
DROP TABLE student
WHERE major = '计算机';
```

D.

```
TRUNCATE TABLE student
WHERE major = '计算机';
```

---

## 八、TRUNCATE与DROP

68. 清空表中全部记录并保留表结构，可以使用（A　）

A. `TRUNCATE TABLE student;`  
B. `DROP TABLE student;`  
C. `DROP DATABASE student;`  
D. `ALTER TABLE student DROP;`

69. 关于 `TRUNCATE TABLE`，正确的是（B　）

A. 可以使用`WHERE`删除指定记录  
B. 只能清空整张表的数据  
C. 会删除数据库  
D. 只删除一条记录

70. 下列语句通常不合法的是（A　）

A.

```
TRUNCATE TABLE student
WHERE major = '计算机';
```

B.

```
DELETE FROM student
WHERE major = '计算机';
```

C.

```
DELETE FROM student;
```

D.

```
TRUNCATE TABLE student;
```

71. 删除表中全部记录且保留表结构的两种方式是（A　）

A. `DELETE FROM`与`TRUNCATE TABLE`  
B. `DROP TABLE`与`DROP DATABASE`  
C. `ALTER TABLE`与`UPDATE`  
D. `INSERT`与`REPLACE`

72. `DELETE FROM student;` 与 `TRUNCATE TABLE student;` 的共同点是（C　）

A. 都能使用`WHERE`  
B. 都会删除表结构  
C. 都可以删除表中的全部记录并保留表结构  
D. 都只能删除指定记录

73. `DELETE` 与 `TRUNCATE` 的重要区别是（A　）

A. `DELETE`可以使用`WHERE`，`TRUNCATE`不能使用`WHERE`  
B. `TRUNCATE`可以使用`WHERE`，`DELETE`不能使用`WHERE`  
C. 二者都会删除表结构  
D. 二者都必须带`WHERE`

74. 大量数据需要全部清空时，通常执行速度较快的是（A　）

A. `TRUNCATE TABLE`  
B. 带条件的`SELECT`  
C. `SHOW TABLES`  
D. `ALTER DATABASE`

75. 清空含有自动增长列的表后，通常会重置自动增长计数器的是（A　）

A. `TRUNCATE TABLE`  
B. 带条件的`UPDATE`  
C. `SELECT`  
D. `SHOW TABLES`

76. 下列关于 `DROP TABLE student;` 的说法正确的是（B　）

A. 只删除数据，保留表结构  
B. 删除表结构以及表中的数据  
C. 只删除满足条件的记录  
D. 只重置自动增长计数器

77. `DELETE`、`TRUNCATE`和`DROP TABLE`的区别正确的是（　）

A. 三者都删除表结构  
B. `DELETE`可删记录，`TRUNCATE`清空记录，`DROP TABLE`删除整张表  
C. `DELETE`只能删表结构  
D. `TRUNCATE`可以删除指定记录

78. 若清空数据后还要继续向原表插入数据，不能使用（　）

A. `DELETE FROM student;`  
B. `TRUNCATE TABLE student;`  
C. `DROP TABLE student;`  
D. 带`WHERE`的`DELETE`

---

## 九、WHERE条件

79. SQL中“不等于”可以写为（　）

A. `<>`或`!=`  
B. `==`或`!==`  
C. `><`或`=!`  
D. `NOT=`

80. 条件`age BETWEEN 18 AND 22`表示（　）

A. 年龄大于18且小于22，不含边界  
B. 年龄在18至22之间，包含边界  
C. 年龄小于18或大于22  
D. 年龄只能等于18

81. `age BETWEEN 18 AND 22`等价于（　）

A. `age > 18 OR age < 22`  
B. `age >= 18 AND age <= 22`  
C. `age = 18 AND age = 22`  
D. `age < 18 AND age > 22`

82. 查询专业属于计算机、软件工程或人工智能的记录，可以使用（　）

A.

```
WHERE major IN ('计算机', '软件工程', '人工智能')
```

B.

```
WHERE major BETWEEN ('计算机', '人工智能')
```

C.

```
WHERE major IS ('计算机', '软件工程')
```

D.

```
WHERE major LIKE IN (...)
```

83. 判断`major`为空值，正确的是（　）

A. `WHERE major = NULL`  
B. `WHERE major IS NULL`  
C. `WHERE major == NULL`  
D. `WHERE major EQUAL NULL`

84. 判断`major`不为空，正确的是（　）

A. `WHERE major <> NULL`  
B. `WHERE major IS NOT NULL`  
C. `WHERE major NOT = NULL`  
D. `WHERE major != NULL`

85. 删除姓名以“张”开头的记录，正确的是（　）

A.

```
DELETE FROM student
WHERE name LIKE '张%';
```

B.

```
DELETE FROM student
WHERE name LIKE '%张';
```

C.

```
DELETE FROM student
WHERE name = '%张%';
```

D.

```
TRUNCATE TABLE student
WHERE name LIKE '张%';
```

86. `LIKE`中的`%`表示（　）

A. 任意一个字符  
B. 任意长度的任意字符  
C. 一个数字  
D. 一个空值

87. `LIKE`中的下划线`_`表示（　）

A. 任意长度的字符  
B. 任意一个字符  
C. 只能表示下划线本身  
D. 任意一个数字且不能是字符

88. 条件`name LIKE '%明'`表示（　）

A. 姓名以“明”开头  
B. 姓名以“明”结尾  
C. 姓名只有一个字“明”  
D. 姓名第二个字一定是“明”

89. 条件`name LIKE '_明'`通常表示（　）

A. 姓名以“明”开头且长度任意  
B. 两个字符且第二个字符是“明”  
C. 姓名中包含任意多个“明”  
D. 姓名为空

90. 筛选计算机或软件工程专业的学生，正确的是（　）

A.

```
WHERE major = '计算机'
   OR major = '软件工程'
```

B.

```
WHERE major = '计算机'
  AND major = '软件工程'
```

C.

```
WHERE major IS '计算机', '软件工程'
```

D.

```
WHERE major = ('计算机' AND '软件工程')
```

---

## 十、约束与安全操作

91. 如果`name`列定义为`NOT NULL`，下列插入通常会失败的是（　）

A.

```
INSERT INTO student (student_id, name)
VALUES ('S010', NULL);
```

B.

```
INSERT INTO student (student_id, name)
VALUES ('S010', '陈一');
```

C.

```
INSERT INTO student (student_id, name, age)
VALUES ('S010', '陈一', 20);
```

D.

```
INSERT INTO student (name, student_id)
VALUES ('陈一', 'S010');
```

92. 如果定义了`CHECK (price >= 0)`，下列修改会违反约束的是（　）

A.

```
UPDATE product
SET price = -10
WHERE product_id = 1;
```

B.

```
UPDATE product
SET price = 10
WHERE product_id = 1;
```

C.

```
UPDATE product
SET price = 0
WHERE product_id = 1;
```

D.

```
UPDATE product
SET price = price + 10
WHERE product_id = 1;
```

93. 删除仍被其他表外键引用的记录时，数据库通常可能（　）

A. 为维护参照完整性而拒绝删除  
B. 自动删除所有数据库  
C. 自动取消所有主键  
D. 自动修改表结构

94. `UPDATE`或`DELETE`遗漏`WHERE`的主要风险是（　）

A. 只能影响一行  
B. 可能影响表中的全部记录  
C. 会自动删除表结构  
D. 会自动关闭数据库

95. 对重要数据执行修改或删除前，使用相同条件进行`SELECT`的目的是（　）

A. 预先确认受影响的记录范围  
B. 修改数据类型  
C. 删除主键  
D. 创建备份表结构

96. 在事务中执行更新后，确认修改无误并使其正式生效，应使用（　）

A. `COMMIT`  
B. `ROLLBACK`  
C. `SHOW`  
D. `DESC`

97. 在事务中执行更新后，希望撤销尚未提交的修改，应使用（　）

A. `GRANT`  
B. `COMMIT`  
C. `ROLLBACK`  
D. `REPLACE`

98. 关于`TRUNCATE`的事务行为，较严谨的说法是（　）

A. 在任何MySQL版本和环境中都一定可以回滚  
B. 应结合MySQL版本和执行环境判断，不能简单认为总能回滚  
C. 它一定与普通`SELECT`完全相同  
D. 它只能在没有表结构时执行

---

## 十一、综合应用题

使用以下商品表回答第99～105题：

```
CREATE TABLE product (
    product_id INT PRIMARY KEY,
    product_name VARCHAR(100) NOT NULL,
    price DECIMAL(10,2) CHECK (price >= 0),
    stock INT DEFAULT 0
);
```

99. 插入一条完整商品记录，正确的是（　）

A.

```
INSERT INTO product
VALUES (1, '机械键盘', 299.00, 50);
```

B.

```
INSERT product
(1, '机械键盘', 299.00, 50);
```

C.

```
UPDATE product
VALUES (1, '机械键盘', 299.00, 50);
```

D.

```
INSERT FROM product
VALUES (1, '机械键盘', 299.00, 50);
```

100. 执行下列语句后，`stock`通常为（　）

```
INSERT INTO product
(product_id, product_name, price)
VALUES (2, '无线鼠标', 129.00);
```

A. `NULL`  
B. `0`  
C. `129`  
D. `2`

101. 同时插入两条商品记录，正确的是（　）

A.

```
INSERT INTO product
(product_id, product_name, price, stock)
VALUES
(3, '显示器', 1299.00, 20),
(4, '移动硬盘', 499.00, 30);
```

B.

```
INSERT INTO product
VALUES (3, '显示器', 1299.00, 20)
VALUES (4, '移动硬盘', 499.00, 30);
```

C.

```
UPDATE product
VALUES
(3, '显示器', 1299.00, 20),
(4, '移动硬盘', 499.00, 30);
```

D.

```
INSERT TWO product
(3, '显示器'), (4, '移动硬盘');
```

102. 将2号商品价格改为119元、库存改为40，正确的是（　）

A.

```
UPDATE product
SET price = 119.00, stock = 40
WHERE product_id = 2;
```

B.

```
UPDATE product
SET price = 119.00 AND stock = 40
WHERE product_id = 2;
```

C.

```
INSERT INTO product
SET price = 119.00, stock = 40;
```

D.

```
ALTER TABLE product
SET price = 119.00;
```

103. 删除库存为0的商品，正确的是（　）

A.

```
DELETE FROM product
WHERE stock = 0;
```

B.

```
DROP TABLE product
WHERE stock = 0;
```

C.

```
TRUNCATE TABLE product
WHERE stock = 0;
```

D.

```
DELETE product
SET stock = 0;
```

104. 将所有商品价格提高5%，正确的是（　）

A.

```
UPDATE product
SET price = price * 1.05;
```

B.

```
UPDATE product
SET price = 1.05;
```

C.

```
ALTER TABLE product
SET price = price + 5;
```

D.

```
UPDATE product
WHERE price = price * 1.05;
```

105. 要迅速清空商品表中的全部记录并保留表结构，可以使用（　）

A. `DROP TABLE product;`  
B. `TRUNCATE TABLE product;`  
C. `DROP DATABASE product;`  
D. `DELETE TABLE product;`