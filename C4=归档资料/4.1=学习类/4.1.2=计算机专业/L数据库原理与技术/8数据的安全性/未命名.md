# 第8章安全管理选择题

以下均为单项选择题，每题只有一个正确答案。

### 1. MySQL 的权限表主要存放在（A ）数据库中。

A. `admin`  
B. `mysql`  
C. `system`  
D. `information_schema`

### 2. MySQL 中保存用户账号和全局权限信息的权限表是（A ）。

A. `mysql.user`  
B. `mysql.db`  
C. `mysql.tables_priv`  
D. `mysql.columns_priv`

### 3. `mysql.db` 表主要保存（B ）。

A. 用户密码  
B. 数据库级权限  
C. 表级权限  
D. 列级权限

### 4. `mysql.tables_priv` 表主要保存（A ）。

A. 表级权限  
B. 数据库级权限  
C. 用户基本信息  
D. 存储过程权限

### 5. `mysql.columns_priv` 表主要保存（C ）。

A. 全局权限  
B. 数据库权限  
C. 列级权限  
D. 角色权限

### 6. `mysql.procs_priv` 表主要与下列哪类权限有关？B

A. 数据库权限  
B. 存储过程和函数权限  
C. 列权限  
D. 用户登录权限

### 7. 用户管理主要包括创建用户、删除用户、修改用户账号和（A ）。

A. 修改用户口令  
B. 修改数据库名称  
C. 修改表结构  
D. 修改服务器地址

### 8. 创建 MySQL 用户应使用（B ）语句。

A. `MAKE USER`  
B. `CREATE USER`  
C. `NEW USER`  
D. `ADD ACCOUNT`

### 9. 下列语句能够正确创建用户的是（A ）。

A. `CREATE USER 'zhangsan'@'localhost' IDENTIFIED BY '123456';`  
B. `CREATE ACCOUNT 'zhangsan'@'localhost' PASSWORD '123456';`  
C. `ADD USER 'zhangsan'@'localhost' IDENTIFIED '123456';`  
D. `NEW USER zhangsan PASSWORD '123456';`

### 10. 在 MySQL 用户表示形式中，`'user'@'host'` 中的 `host` 表示（B ）。

A. 用户的密码  
B. 用户名  
C. 用户允许登录的主机  
D. 用户的默认数据库

### 11. `'zhangsan'@'localhost'` 表示用户可以从（B ）登录。

A. 任意主机  
B. 本地主机  
C. 指定数据库服务器以外的主机  
D. 只能从远程主机登录

### 12. 若希望用户可以从任意主机连接，通常可以使用（D ）。

A. `'user'@'localhost'`  
B. `'user'@'127.0.0.1'`  
C. `'user'@'%'`  
D. `'%'@'user'`

### 13. 删除用户应使用（B ）语句。

A. `DELETE USER`  
B. `DROP USER`  
C. `REMOVE USER`  
D. `CLEAR USER`

### 14. 下列语句能够正确删除用户的是（A ）。

A. `DROP USER 'lisi'@'localhost';`  
B. `DELETE USER 'lisi'@'localhost';`  
C. `REMOVE ACCOUNT 'lisi'@'localhost';`  
D. `DROP ACCOUNT lisi;`

### 15. 删除用户时，如果用户不存在，想避免语句报错，可以使用（A ）。

A. `DROP USER IF EXISTS`  
B. `DELETE USER IF EXISTS`  
C. `REMOVE USER EXISTS`  
D. `DROP ACCOUNT EXISTS ONLY`

### 16. 修改用户账号名称应使用（ B）语句。

A. `CHANGE USER`  
B. `RENAME USER`  
C. `UPDATE USER`  
D. `ALTER ACCOUNT NAME`

### 17. 下列语句能够正确修改用户账号的是（A ）。

A. `RENAME USER 'old'@'localhost' TO 'new'@'localhost';`  
B. `RENAME ACCOUNT 'old' TO 'new';`  
C. `ALTER USER NAME 'old' TO 'new';`  
D. `UPDATE USER 'old'@'localhost' AS 'new'@'localhost';`

### 18. 修改 MySQL 用户口令可以使用（A ）语句。

A. `SET PASSWORD`  
B. `CHANGE PASSWORD ONLY`  
C. `MODIFY PASSWD`  
D. `UPDATE LOGIN KEY`

### 19. 下列语句可以修改指定用户口令的是（A ）。

A. `SET PASSWORD FOR 'zhangsan'@'localhost' = 'abc123';`  
B. `SET PASSWD 'zhangsan'@'localhost' TO 'abc123';`  
C. `MODIFY PASSWORD FOR 'zhangsan' = 'abc123';`  
D. `UPDATE PASSWORD 'abc123' FOR USER zhangsan;`

### 20. 用户修改自己的当前登录口令时，可以使用（A ）。

A. `SET PASSWORD = 'new_password';`  
B. `SET MY PASSWORD TO 'new_password';`  
C. `CHANGE PASSWORD SELF 'new_password';`  
D. `MODIFY CURRENT USER PASSWORD 'new_password';`

### 21. 用户口令在 SQL 中通常应使用（C ）括起来。

A. 圆括号  
B. 方括号  
C. 单引号  
D. 花括号

### 22. 权限管理主要包括授予权限和（B ）。

A. 删除数据库  
B. 撤销权限  
C. 修改表名  
D. 创建索引

### 23. 向用户授予权限应使用（ B）语句。

A. `GIVE`  
B. `GRANT`  
C. `ALLOW`  
D. `PERMIT`

### 24. 撤销用户权限应使用（B ）语句。

A. `CANCEL`  
B. `REVOKE`  
C. `DENY`  
D. `REMOVE PRIVILEGE`

### 25. 下列语句能够授予用户查询权限的是（A ）。

A. `GRANT SELECT ON *.* TO 'user1'@'localhost';`  
B. `GIVE QUERY ON *.* TO user1;`  
C. `ALLOW SELECT FROM *.* TO user1;`  
D. `GRANT QUERY TO user1;`

### 26. 下列语句能够撤销用户查询权限的是 B（ ）。

A. `REVOKE SELECT ON *.* FROM 'user1'@'localhost';`  
B. `REVOKE QUERY ON *.* TO 'user1'@'localhost';`  
C. `CANCEL SELECT FROM user1;`  
D. `DROP SELECT PRIVILEGE FROM user1;`

### 27. `GRANT ALL PRIVILEGES ON *.* TO 'user1'@'localhost';` 中的 `*.*` 表示（C ）。

A. 当前表的所有列  
B. 所有数据库中的所有对象  
C. 当前数据库中的所有表  
D. 所有用户的所有权限

### 28. `GRANT SELECT ON school.* TO 'user1'@'localhost';` 表示授予用户（B ）。

A. 所有数据库的查询权限  
B. `school` 数据库中所有表的查询权限  
C. `school` 表中所有列的查询权限  
D. 仅 `school` 数据库创建权限

### 29. `GRANT SELECT ON school.student TO 'user1'@'localhost';` 表示授予用户（B ）。

A. `school` 数据库中所有表的查询权限  
B. `student` 表的查询权限  
C. `student` 数据库的查询权限  
D. 所有数据库中 `student` 表的查询权限

### 30. 下列权限级别表示数据库级权限的是（B ）。

A. `*.*`  
B. `db_name.*`  
C. `db_name.table_name`  
D. `db_name.table_name(column_name)`

### 31. 下列权限级别表示表级权限的是（C ）。

A. `*.*`  
B. `db_name.*`  
C. `db_name.table_name`  
D. `table_name.*`

### 32. 下列权限级别表示列级权限的是（A ）。

A. `db_name.table_name(column_name)`  
B. `db_name.*`  
C. `*.*`  
D. `column_name.*`

### 33. 下列语句用于授予用户对 `student` 表中 `name` 列和 `age` 列的查询权限，正确的是（A ）。

A.

```
GRANT SELECT (name, age)
ON school.student
TO 'user1'@'localhost';
```

B.

```
GRANT COLUMN SELECT name, age
ON school.student
TO 'user1'@'localhost';
```

C.

```
GRANT SELECT name, age
TO 'user1'@'localhost';
```

D.

```
GRANT school.student (name, age)
TO 'user1'@'localhost';
```

### 34. `GRANT INSERT, UPDATE ON school.student TO ...` 中的 `INSERT` 和 `UPDATE` 属于（B ）。

A. 用户账号  
B. 权限名称  
C. 数据库名称  
D. 主机名称

### 35. 下列哪一项属于数据操作类权限？B

A. `SELECT`  
B. `CREATE USER`  
C. `RELOAD`  
D. `SHUTDOWN`

### 36. `SELECT` 权限的含义是（B ）。

A. 创建数据库  
B. 查询数据  
C. 删除用户  
D. 修改用户密码

### 37. `INSERT` 权限的含义是（ A）。

A. 插入数据  
B. 查询数据  
C. 删除表  
D. 创建用户

### 38. `UPDATE` 权限的含义是（ A）。

A. 修改数据  
B. 修改用户账号  
C. 更新服务器  
D. 修改数据库密码

### 39. `DELETE` 权限的含义是（A ）。

A. 删除数据  
B. 删除用户  
C. 删除数据库服务器  
D. 删除权限表

### 40. `CREATE` 权限主要允许用户（A ）。

A. 创建数据库或表等对象  
B. 查询所有用户密码  
C. 修改其他用户账号  
D. 关闭 MySQL 服务

### 41. `DROP` 权限主要允许用户（ A）。

A. 删除数据库对象  
B. 修改数据  
C. 查询数据  
D. 创建用户账号

### 42. `ALTER` 权限主要允许用户（A ）。

A. 修改数据库对象结构  
B. 修改用户密码  
C. 查询系统日志  
D. 删除用户权限

### 43. `GRANT OPTION` 权限的作用是（A ）。

A. 允许用户将自己拥有的权限授予其他用户  
B. 允许用户修改自己的用户名  
C. 允许用户创建数据库  
D. 允许用户登录任意主机

### 44. 下列语句授予用户查询权限，并允许其继续授予该权限的是（ ）。

A.

```
GRANT SELECT ON school.* 
TO 'user1'@'localhost'
WITH GRANT OPTION;
```

B.

```
GRANT SELECT WITH OPTION 
ON school.* TO 'user1'@'localhost';
```

C.

```
GRANT SELECT ON school.* 
TO 'user1'@'localhost'
WITH USER OPTION;
```

D.

```
GRANT SELECT AND GRANT 
ON school.* TO 'user1'@'localhost';
```

### 45. 如果只想撤销用户的授权转授能力，可以使用（ ）。

A. `REVOKE GRANT OPTION ON ... FROM ...`  
B. `REVOKE ALL ON ... FROM ...`  
C. `DROP GRANT OPTION ...`  
D. `REMOVE USER OPTION ...`

### 46. 下列语句可以撤销用户在 `school` 数据库中的全部权限的是（ ）。

A.

```
REVOKE ALL PRIVILEGES ON school.* 
FROM 'user1'@'localhost';
```

B.

```
REVOKE DATABASE ON school.* 
FROM 'user1'@'localhost';
```

C.

```
DELETE ALL ON school.* 
FROM 'user1'@'localhost';
```

D.

```
DROP PRIVILEGES ON school.* 
FROM 'user1'@'localhost';
```

### 47. 授予用户权限前，通常要求该用户（ ）。

A. 已经通过 `CREATE USER` 创建  
B. 必须先创建数据库  
C. 必须先创建表  
D. 必须先修改服务器配置

### 48. 下列关于 `GRANT` 和 `REVOKE` 的说法，正确的是（ ）。

A. `GRANT` 用于撤销权限  
B. `REVOKE` 用于授予权限  
C. `GRANT` 用于授予权限，`REVOKE` 用于撤销权限  
D. 二者功能完全相同

### 49. 下列关于用户和权限的说法，正确的是（ ）。

A. 删除用户后，该用户账号仍然可以正常登录  
B. 用户名相同但主机不同，可能被 MySQL 视为不同用户  
C. 所有用户默认拥有全部权限  
D. 创建用户后自动拥有所有数据库权限

### 50. 为用户授予某个数据库全部权限时，正确的权限级别格式是（ ）。

A. `数据库名.*`  
B. `数据库名.#`  
C. `数据库名.ALL`  
D. `*数据库名`

---

# 参考答案

1. B
2. A
3. B
4. A
5. C
6. B
7. A
8. B
9. A
10. C
11. B
12. C
13. B
14. A
15. A
16. B
17. A
18. A
19. A
20. A
21. C
22. B
23. B
24. B
25. A
26. A
27. B
28. B
29. B
30. B
31. C
32. A
33. A
34. B
35. A
36. B
37. A
38. A
39. A
40. A
41. A
42. A
43. A
44. A
45. A
46. A
47. A
48. C
49. B
50. A