# 《程序设计入门-Java》综合模拟试卷 (V3.0 限定范围增强版)

（课程代码：08553）

**考试说明：** 本试卷题目范围严格限定在您提供的第一章至第八章学习笔记内容，题型包括单项选择题、填空题、程序分析/填空题、简答题和程序设计题。

---

## 第一部分：单项选择题 (共10题)

**1. (第一章)** 以下关于Java开发与运行环境的说法，哪项是正确的？C
   A. 编写Java程序必须安装JRE。
   B. 只有安装了JDK才能编译Java源代码。
   C. JRE中包含了JVM，但JVM可以独立于JRE运行。
   D. `javac` 命令用于运行Java字节码文件。

**2. (第二章)** Java语言中，下列哪个标识符的命名是**不合法**的？C
   A. `my_Variable`
   B. `_name`
   C. `public`
   D. `$price`

**3. (第二章)** 以下Java代码片段中，哪个语句会导致编译错误？C吧，127上限？
   A. `long num = 9999999999L;`
   B. `float price = 19.9f;`
   C. `byte b = 128;`
   D. `char c = '中';`

**4. (第三章)** 下列关于`switch`语句的描述，哪项是**错误**的？B，可以支持什么内容，我忘记了。
   A. `switch`语句可以用于判断一个字符串的值。
   B. `switch`语句的表达式可以是 `long` 类型。
   C. `case`后面的值必须是常量。
   D. 如果没有`break`，程序会继续执行下一个`case`的代码。

**5. (第三章)** 执行以下Java代码，输出结果是？D
   ```java
   int i = 0;
   while (i < 5) {
       if (i % 2 == 0) {
           i++;
           continue;
       }
       System.out.print(i + " ");
       i++;
   }
   ```
   A. `0 1 2 3 4 `
   B. `1 3 `
   C. `0 2 4 `
   D. `0 1 2 3 `

**6. (第四章)** 以下代码中，获取数组 `arr` 长度的正确方式是？D
   ```java
   int[] arr = {10, 20, 30, 40};
   ```
   A. `arr.length()`
   B. `arr.size()`
   C. `arr.getLength()`
   D. `arr.length`

**7. (第五章)** 关于Java中的`static`关键字，以下说法哪项是**错误**的？D，不确定这个，我用的排除法，需要详细点解析
   A. `static`方法中不能直接访问非`static`的成员变量。
   B. `static`成员属于类，被所有对象共享。
   C. `static`方法可以在不创建对象的情况下，通过类名直接调用。
   D. `static`方法可以被子类重写（Override）。

**8. (第六章)** 以下哪个选项不属于Java面向对象的三大特征？D
   A. 封装
   B. 继承
   C. 多态
   D. 抽象

**9. (第六章)** 以下代码段中，关于`instanceof`运算符的说法，哪项是正确的？B
   ```java
   Object obj = new String("hello");
   ```
   A. `obj instanceof Object` 的结果是 `false`。
   B. `obj instanceof String` 的结果是 `true`。
   C. `obj instanceof Integer` 的结果是 `true`。
   D. `obj instanceof Comparable` 会导致编译错误。

**10. (第七章)** 下列关于异常处理的说法，哪项是**错误**的？D
    A. `try` 块中可能抛出异常的代码。
    B. `catch` 块可以有多个，用于捕获不同类型的异常。
    C. `finally` 块中的代码总是会执行，即使`try`或`catch`中有`return`语句。
    D. `throws` 关键字用于在方法体内部手动抛出一个异常对象。

---

## 第二部分：填空题 (共6题)

**11. (第一章)** Java语言是__半编译半执行______（填“半编译半解释”或“纯编译”）的语言，它首先将源代码编译成字节码文件，然后由________（填“JVM”或“JRE”）负责解释执行。

**12. (第二章)** 在Java中，一个`byte`类型的变量，其取值范围是 __-127~127______ 到 ________。

**13. (第三章)** `do-while`循环与`while`循环的主要区别在于，`do-while`循环至少会执行__1______次循环体。

**14. (第四章)** 在Java中，可以使用`Arrays.___toString_____(arr)`方法将一个整型数组`arr`的内容以字符串形式打印出来，方便调试。

**15. (第五章)** 如果一个类没有显式地继承任何其他类，那么它默认继承自__Object______类。

**16. (第八章)** 在Java中，`int` 对应的包装类是 `Integer`，那么 `boolean` 对应的包装类是 __这个忘了，其他所有类型的都忘了______。

---

## 第三部分：程序分析/填空题 (共5题)

**17. (第三章/代码填空)** 请阅读以下 `NumberClassifier.java` 文件内容，并在 `main` 方法的空白处填入代码，使得当输入的数字为偶数时，打印“这是一个偶数”，否则打印“这是一个奇数”。
错了，填空题一般是填一个关键字或者核心逻辑关键代码，不是直接填入代码
   ```java
   import java.util.Scanner;

   public class NumberClassifier {
       public static void main(String[] args) {
           Scanner sc = new Scanner(System.in);
           System.out.println("请输入一个整数：");
           int i = sc.nextInt();

           // 在这里填入代码
           ____________________________________
           ____________________________________
           ____________________________________
           ____________________________________
           ____________________________________
           
           sc.close();
       }
   }
   ```

**18. (第四章/程序分析)** 阅读以下`ArrayMaxFinder.java` 代码，请写出当`main`方法中数组为`{5, 2, 8, 1, 9, 4}`时，程序的输出结果。

   ```java
   public class ArrayMaxFinder {
       public static int findMax(int[] arr) {
           if (arr == null || arr.length == 0) {
               return -1; // 或者抛出异常
           }
           int max = arr[0];
           for (int i = 1; i < arr.length; i++) {
               if (arr[i] > max) {
                   max = arr[i];
               }
           }
           return max;
       }

       public static void main(String[] args) {
           int[] numbers = {5, 2, 8, 1, 9, 4};
           int maxValue = findMax(numbers);
           System.out.println("数组中的最大值是: " + maxValue);
       }
   }
   ```
   输出结果：___9_，这种自己算都就能算出来的题，考试不会考的。可以加点难度，你没有根据大纲的要求生成代码题________________________________

**19. (第五章/代码填空)** 请完善以下 `Calculator.java` 代码，使其能够实现两个整数的加法运算。

   ```java
   public class Calculator {
       // 请在这里添加一个静态的加法方法
       // ____________________________________
       // ____________________________________
       // ____________________________________

       public static void main(String[] args) {
           int result = add(10, 5);
           System.out.println("10 + 5 = " + result); // 期望输出: 10 + 5 = 15
       }
   }
   ```

**20. (第六章/程序分析)** 阅读以下代码，请写出程序的输出结果。

   ```java
   class Animal {
       public void sound() {
           System.out.println("Animal makes a sound");
       }
   }

   class Dog extends Animal {
       @Override
       public void sound() {
           System.out.println("Dog barks");
       }
       public void fetch() {
           System.out.println("Dog fetches");
       }
   }

   public class TestPolymorphism {
       public static void main(String[] args) {
           Animal myAnimal = new Dog();
           myAnimal.sound();
           // myAnimal.fetch(); // 假设这行代码存在，会发生什么？
       }
   }
   ```
   输出结果：Dog barks____________________________________

**21. (第七章/代码填空)** 请完善以下异常处理代码，使得当发生除数为零的异常时，能够打印出提示信息“除数不能为零！”，并且无论是否发生异常，最终都打印“程序执行完毕。”

   ```java
   public class ExceptionDemo {
       public static void main(String[] args) {
           try {
               int result = 10 / 0; // 这行代码会抛出 ArithmeticException
               System.out.println("结果：" + result);
           } __catch______ (________ e) { // 捕获异常
               System.out.println("除数不能为零！");
           } __finial______ { // 最终执行
               System.out.println("程序执行完毕。");
           }
       }
   }
   ```

---

## 第四部分：简答题 (共3题)

**22. (第一章)** 请简述JDK、JRE和JVM三者之间的关系。


**23. (第三章)** 请简述`break`和`continue`关键字在循环结构中的作用和区别。

**24. (第八章)** 请简述Java中`String`类和`StringBuffer`类的主要区别是什么？它们各自适用于什么场景？

---

## 第五部分：程序设计题 (共2题)

**25. (第四章)** 编写一个Java方法，该方法接收一个整型数组 `int[] arr` 和一个整数 `int target`作为参数，返回 `target` 在数组中第一次出现的索引。如果数组中不存在该元素，则返回 -1。

   ```java
   // 请补充以下方法
   public int findIndex(int[] arr, int target) {
       // Your code here
   }
   ```

**26. (综合应用 - 第五章/第八章)** 请设计一个`Product`类，包含以下私有属性：
    *   `name` (String): 产品名称
    *   `price` (double): 产品价格

    要求：
    1.  提供一个接收名称和价格的构造方法。
    2.  为这两个属性提供公开的 `get` 方法。
    3.  重写 `toString()` 方法，使其返回类似 "Product{name='键盘', price=199.0}" 的格式。
    4.  重写 `equals()` 方法，规定当两个`Product`对象的名称和价格都相同时，认为这两个产品是相同的。
