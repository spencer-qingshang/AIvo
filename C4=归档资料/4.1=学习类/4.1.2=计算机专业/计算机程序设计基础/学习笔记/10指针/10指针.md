# 第十章 指针

- 状态：未开始
- 来源：13793《计算机程序设计基础》自学考试大纲

## 考纲内容

指针和指针变量；指针和数组；指针和字符串；指针和函数。函数指针变量属于扩展阅读，不作为考试内容。

## 考核要求

- 指针的加、减、关系运算，直接和间接引用；定义、赋值、初始化及 `&`、`*`。
- 访问数组元素的四种方法；指向一维数组元素的指针。
- 字符串指针变量及访问字符串常量、字符数组元素。
- 数组名或指针变量作为函数参数。

## 重点与难点

重点：指针变量、指向数组元素的指针变量。  
难点：指针变量作为函数参数。
