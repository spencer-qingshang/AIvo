# 第三章 MySQL 数据库与表的定义——单项选择题

（每小题有 4 个备选项，只有一个符合题目要求。建议先独立作答，再把答案发给我；批改错题时，我会附上原题、全部选项和详细解析。）

## 一、MySQL 与数据库基本操作

1. MySQL 属于（B　）

A. 操作系统  
B. 关系数据库管理系统  
C. 程序设计语言  
D. 文本编辑器

2. 查看 MySQL 服务器中已有数据库，应使用（C　）

A. `SHOW TABLES;`  
B. `SHOW DATABASE;`  
C. `SHOW DATABASES;`  
D. `SELECT DATABASES;`

3. 下列通常属于 MySQL 系统数据库的是（A　）

A. `information_schema`  
B. `student`  
C. `school`  
D. `course`

4. SQL 语句通常使用哪个符号表示结束？（B　）

A. 逗号 `,`  
B. 分号 `;`  
C. 冒号 `:`  
D. 句号 `.`

5. 下列创建数据库的语句中，正确的是（B　）

A. `CREATE school DATABASE;`  
B. `CREATE DATABASE school;`  
C. `DATABASE CREATE school;`  
D. `BUILD DATABASE school;`

6. 为避免数据库已经存在而导致创建操作报错，可使用（C　）

A. `CREATE DATABASE IF EXISTS school;`  
B. `CREATE IF NOT EXISTS DATABASE school;`  
C. `CREATE DATABASE IF NOT EXISTS school;`  
D. `IF NOT EXISTS CREATE DATABASE school;`

7. 创建数据库 `teaching` 并指定字符集为 `utf8mb4`，正确的是（A　）

A. `CREATE DATABASE teaching CHARACTER SET utf8mb4;`  
B. `CREATE DATABASE teaching TYPE utf8mb4;`  
C. `CREATE DATABASE teaching DATA SET utf8mb4;`  
D. `CREATE DATABASE teaching ENGINE utf8mb4;`

8. `COLLATE` 子句主要用于指定数据库的（C　）

A. 存储引擎  
B. 用户访问权限  
C. 字符比较和排序规则  
D. 数据文件保存位置

9. 查看数据库 `school` 的创建定义，应使用（C　）

A. `SHOW DATABASE school;`  
B. `DESC DATABASE school;`  
C. `SHOW CREATE DATABASE school;`  
D. `SHOW DATABASES school;`

10. 将 `school` 设置为当前数据库，应使用（C　）

A. `SELECT school;`  
B. `OPEN school;`  
C. `USE school;`  
D. `SHOW school;`

11. `USE school;` 语句的作用是（B　）

A. 创建数据库 `school`  
B. 选择数据库 `school` 作为当前数据库  
C. 删除数据库 `school`  
D. 修改数据库 `school`

12. 查看当前正在使用的数据库，可执行（B　）

A. `SELECT DATABASE();`  
B. `SHOW CURRENT TABLE;`  
C. `SELECT TABLE();`  
D. `USE DATABASE();`

13. 没有选择当前数据库便直接执行 `CREATE TABLE`，最可能出现的提示是（C　）

A. 数据库已经存在  
B. 表中没有主键  
C. 没有选择数据库  
D. 字符集不存在

14. 修改数据库默认字符集通常使用（C　）

A. `UPDATE DATABASE`  
B. `MODIFY DATABASE`  
C. `ALTER DATABASE`  
D. `CHANGE DATABASE`

15. 将数据库 `school` 的默认字符集修改为 `utf8mb4`，正确的是（A　）

A. `ALTER DATABASE school CHARACTER SET utf8mb4;`  
B. `ALTER TABLE school CHARACTER SET utf8mb4;`  
C. `UPDATE DATABASE school SET utf8mb4;`  
D. `CHANGE DATABASE school TO utf8mb4;`

16. 关于 `ALTER DATABASE`，下列说法正确的是（A　）

A. 主要用于修改数据库的默认字符集、排序规则等属性  
B. 主要用于修改表中的具体记录  
C. 主要用于查询数据库中的数据  
D. 可以直接修改任意表的列名

17. 删除数据库 `school` 的正确语句是（B　）

A. `DELETE DATABASE school;`  
B. `DROP DATABASE school;`  
C. `REMOVE DATABASE school;`  
D. `DROP school DATABASE;`

18. 为避免删除一个不存在的数据库时报错，可以使用（A　）

A. `DROP DATABASE IF EXISTS school;`  
B. `DROP IF EXISTS DATABASE school;`  
C. `DELETE DATABASE IF EXISTS school;`  
D. `DROP DATABASE IF NOT EXISTS school;`

19. 执行 `DROP DATABASE school;` 后，通常会（C　）

A. 只删除数据库名称，保留其中的表  
B. 只删除表中数据，保留所有表结构  
C. 删除数据库及其中的数据库对象和数据  
D. 只取消当前数据库的选择状态

20. 下列操作顺序最合理的是（C　）

A. `CREATE TABLE` → `USE` → `CREATE DATABASE`  
B. `USE` → `DROP DATABASE` → `CREATE TABLE`  
C. `CREATE DATABASE` → `USE` → `CREATE TABLE`  
D. `SHOW TABLES` → `DROP DATABASE` → `USE`

---

## 二、表与表结构

21. 在关系数据库中，组织和存储数据的基本单位通常是（A　）

A. 表  
B. 程序  
C. 文件夹  
D. 用户

22. 数据表中的一行通常称为（A　）

A. 属性或字段  
B. 记录或元组  
C. 域  
D. 表结构

23. 数据表中的一列通常称为（A　）

A. 记录或元组  
B. 数据库  
C. 字段或属性  
D. 索引文件

24. 表中的一列与关系模型中的什么概念相对应？（B　）

A. 元组  
B. 属性  
C. 关系实例  
D. 基数

25. 下列属于表结构内容的是（B　）

A. 表中某名学生的具体成绩  
B. 表的列名、数据类型和完整性约束  
C. 当前查询返回的所有数据  
D. 用户登录数据库的时间

26. 下列不属于表结构组成内容的是（D　）

A. 列名  
B. 数据类型  
C. 完整性约束  
D. 某行当前保存的具体值

27. 关于表结构和表数据，下列说法正确的是（C　）

A. 表结构是表中当前保存的具体记录  
B. 表数据规定列的数据类型和约束  
C. 表结构描述表如何组织，表数据是具体记录  
D. 表结构和表数据完全相同

28. 修改表结构通常使用（B　）

A. `UPDATE TABLE`  
B. `ALTER TABLE`  
C. `CHANGE DATA`  
D. `MODIFY DATA`

29. 修改表中已有记录的值通常使用（A　）

A. `UPDATE`  
B. `ALTER TABLE`  
C. `CREATE TABLE`  
D. `SHOW TABLES`

30. 下列说法正确的是（C　）

A. `ALTER TABLE` 用于修改表中具体数据  
B. `UPDATE` 用于修改表结构  
C. `ALTER TABLE` 修改表结构，`UPDATE` 修改表中数据  
D. `ALTER TABLE` 和 `UPDATE` 的作用完全相同

---

## 三、常用数据类型

31. 下列属于 MySQL 整数类型的是（C　）

A. `VARCHAR`  
B. `DATE`  
C. `INT`  
D. `DECIMAL`

32. 下列整数类型中，通常用于保存取值范围很大的整数的是（D　）

A. `TINYINT`  
B. `SMALLINT`  
C. `INT`  
D. `BIGINT`

33. 如果年龄只允许使用非负整数表示，较合理的类型是（A　）

A. `TINYINT UNSIGNED`  
B. `VARCHAR(200)`  
C. `DOUBLE`  
D. `DATE`

34. 数字类型中的 `UNSIGNED` 表示（D　）

A. 不允许使用整数  
B. 不允许出现负数  
C. 数值必须为小数  
D. 数据不能进行比较

35. 在能够满足数据取值范围的条件下，整数类型的选择通常应（D　）

A. 一律选择 `BIGINT`  
B. 一律选择 `TINYINT`  
C. 尽量选择合适且较小的类型  
D. 使用字符串代替整数

36. 保存商品价格，最适合的数据类型是（C　）

A. `CHAR(10)`  
B. `DATE`  
C. `DECIMAL(10,2)`  
D. `BLOB`

37. `DECIMAL(10,2)` 中的 `2` 表示（　）

A. 最多保存 2 个字符  
B. 小数点后最多有 2 位  
C. 整数部分最多有 2 位  
D. 该列只能保存两个数值

38. 对于 `DECIMAL(10,2)`，整数部分最多可以有（　）

A. 2 位  
B. 8 位  
C. 10 位  
D. 12 位

39. 科学测量结果允许存在微小误差时，可以选择（　）

A. `FLOAT` 或 `DOUBLE`  
B. `CHAR` 或 `VARCHAR`  
C. `DATE` 或 `TIME`  
D. `BLOB` 或 `TEXT`

40. 下列关于 `DECIMAL` 与 `FLOAT` 的叙述，正确的是（　）

A. `DECIMAL` 属于近似数值类型  
B. 金额通常优先使用 `FLOAT`  
C. `DECIMAL` 适合要求精确的小数  
D. 两者都只能保存整数

41. 保存固定长度的学生编号，较适合使用（　）

A. `CHAR`  
B. `TEXT`  
C. `DOUBLE`  
D. `DATE`

42. 保存长度变化较大的学生姓名，较适合使用（　）

A. `BIGINT`  
B. `VARCHAR`  
C. `TIME`  
D. `BLOB`

43. 关于 `CHAR(n)` 和 `VARCHAR(n)`，下列说法正确的是（　）

A. `CHAR` 是变长字符串，`VARCHAR` 是定长字符串  
B. 二者都只能保存数字  
C. `CHAR` 是定长字符串，`VARCHAR` 是变长字符串  
D. 二者都用于保存日期

44. 保存一篇较长的文章内容，较适合使用（　）

A. `TINYINT`  
B. `TEXT`  
C. `YEAR`  
D. `DECIMAL`

45. 只保存“年—月—日”的出生日期，较适合使用（　）

A. `TIME`  
B. `DATE`  
C. `YEAR`  
D. `INT`

46. 保存“2026-09-08 14:30:00”这样的日期和时间，较适合使用（　）

A. `DATETIME`  
B. `DATE`  
C. `TIME`  
D. `CHAR(1)`

47. 只保存一天中的具体时刻，较适合使用（　）

A. `DATE`  
B. `TIME`  
C. `YEAR`  
D. `TEXT`

48. 只保存年份，较适合使用（　）

A. `YEAR`  
B. `TIME`  
C. `BLOB`  
D. `FLOAT`

49. 保存较大的二进制数据可以使用（　）

A. `VARCHAR`  
B. `BLOB`  
C. `DATE`  
D. `DECIMAL`

50. 关于 MySQL 中的 `BOOLEAN`，下列说法正确的是（　）

A. 通常可用 0 和 1 表示假与真  
B. 只能保存字符串 `TRUE` 和 `FALSE`  
C. 用于保存长文本  
D. 用于保存日期和时间

51. 电话号码虽然主要由数字组成，但通常更适合使用（　）

A. `CHAR` 或 `VARCHAR`  
B. `FLOAT`  
C. `DECIMAL`  
D. `DATE`

52. 身份证号通常不宜使用数值类型，主要是因为（　）

A. 身份证号需要进行小数运算  
B. 身份证号可能包含字符且不用于算术运算  
C. 数值类型不能进行比较  
D. 字符类型的取值范围一定更小

53. 如果需要对日期进行排序、比较和日期计算，通常应（　）

A. 使用 `VARCHAR` 保存日期  
B. 使用专门的日期时间类型  
C. 使用 `BLOB` 保存日期  
D. 将日期拆成多个 `TEXT` 字段

54. 数据类型选择的一般原则不包括（　）

A. 能正确表达数据含义  
B. 满足数据取值范围  
C. 兼顾存储空间  
D. 所有字段一律使用最长的字符串类型

---

## 四、数据完整性与约束

55. 数据完整性主要是指数据的（　）

A. 正确性、有效性和一致性  
B. 字体、字号和颜色  
C. 文件名和保存路径  
D. 查询速度和显示顺序

56. 用于唯一标识表中一条记录的约束是（　）

A. `DEFAULT`  
B. `PRIMARY KEY`  
C. `CHECK`  
D. `FOREIGN KEY`

57. 关于主键，下列说法正确的是（　）

A. 主键可以重复，也可以为 `NULL`  
B. 主键只能限制字符类型  
C. 主键值不能重复且不能为 `NULL`  
D. 一张表可以同时设置多个互相独立的主键

58. 要求学生姓名必须填写，可使用（　）

A. `UNIQUE`  
B. `DEFAULT`  
C. `NOT NULL`  
D. `UNSIGNED`

59. 要求身份证号不能重复，可使用（　）

A. `UNIQUE`  
B. `DEFAULT`  
C. `CHECK`  
D. `UNSIGNED`

60. 插入数据时没有指定性别，希望系统自动使用“男”，可使用（　）

A. `PRIMARY KEY`  
B. `DEFAULT '男'`  
C. `FOREIGN KEY`  
D. `UNIQUE '男'`

61. 限制成绩必须在 0～100 之间，可使用（　）

A. `CHECK (score BETWEEN 0 AND 100)`  
B. `DEFAULT (score BETWEEN 0 AND 100)`  
C. `UNIQUE (score BETWEEN 0 AND 100)`  
D. `PRIMARY KEY (score BETWEEN 0 AND 100)`

62. 外键约束主要用于保证（　）

A. 实体完整性  
B. 参照完整性  
C. 表名完整性  
D. 存储完整性

63. 外键的主要作用是（　）

A. 唯一标识本表中的每条记录  
B. 建立并约束表与表之间的引用关系  
C. 自动删除所有重复数据  
D. 设置字段的默认值

64. 创建外键时，被引用的列通常应当是（　）

A. 主键或唯一键  
B. 任意可重复列  
C. `TEXT` 列  
D. 允许任意取值的列

65. 创建外键时，下列说法错误的是（　）

A. 被引用表通常应当已经存在  
B. 外键列和被引用列的数据类型应兼容  
C. 存储引擎需要支持外键  
D. 外键值可以任意引用不存在的值

66. 关系“选课（学号，课程号，成绩）”中，同一学生不能重复选修同一课程，较合理的主键是（　）

A. 只以学号为主键  
B. 只以课程号为主键  
C. 以学号和课程号组成联合主键  
D. 以成绩为主键

67. 下列属于实体完整性实现方式的是（　）

A. `PRIMARY KEY`  
B. `FOREIGN KEY`  
C. `DEFAULT`  
D. `CHECK`

68. 下列更能体现用户定义完整性的是（　）

A. 主键不能为空  
B. 外键必须引用已经存在的主键  
C. 年龄必须在 1～120 之间  
D. 一张表必须有表名

---

## 五、创建与查看表

69. 创建表应使用（　）

A. `CREATE TABLE`  
B. `CREATE DATABASE`  
C. `ALTER TABLE`  
D. `SHOW TABLES`

70. 下列创建学生表的语句中，正确的是（　）

A. `CREATE TABLE student (id INT, name VARCHAR(20));`  
B. `CREATE student TABLE (id INT, name VARCHAR(20));`  
C. `TABLE CREATE student (id INT, name VARCHAR(20));`  
D. `CREATE DATABASE student (id INT, name VARCHAR(20));`

71. 下列能够正确设置列级主键的是（　）

A. `student_id CHAR(4) PRIMARY KEY`  
B. `student_id PRIMARY KEY CHAR(4)`  
C. `PRIMARY student_id KEY CHAR(4)`  
D. `student_id CHAR(4) FOREIGN KEY`

72. 下列能够正确设置表级主键的是（　）

A. `KEY PRIMARY student_id`  
B. `PRIMARY KEY (student_id)`  
C. `PRIMARY (student_id) KEY`  
D. `KEY (student_id) PRIMARY`

73. 创建联合主键的正确写法是（　）

A. `PRIMARY KEY student_id, course_id`  
B. `PRIMARY KEY (student_id), (course_id)`  
C. `PRIMARY KEY (student_id, course_id)`  
D. `PRIMARY (student_id AND course_id)`

74. 为避免表已存在时报错，可以使用（　）

A. `CREATE TABLE IF NOT EXISTS student (...);`  
B. `CREATE IF NOT EXISTS TABLE student (...);`  
C. `CREATE TABLE IF EXISTS student (...);`  
D. `IF EXISTS CREATE TABLE student (...);`

75. 创建表时指定 `InnoDB` 存储引擎，应使用（　）

A. `CHARACTER SET = InnoDB`  
B. `ENGINE = InnoDB`  
C. `COLLATE = InnoDB`  
D. `TYPE = DATABASE`

76. 查看当前数据库中的所有表，应使用（　）

A. `SHOW DATABASES;`  
B. `SHOW TABLES;`  
C. `SHOW COLUMNS;`  
D. `SELECT TABLES;`

77. 在没有切换当前数据库的情况下，查看 `school` 数据库中的表，可以使用（　）

A. `SHOW TABLES FROM school;`  
B. `SHOW DATABASES FROM school;`  
C. `DESC TABLES FROM school;`  
D. `SELECT TABLES school;`

78. 查看 `student` 表的基本结构，可以使用（　）

A. `USE student;`  
B. `DESC student;`  
C. `SHOW DATABASE student;`  
D. `SELECT STRUCTURE student;`

79. `DESCRIBE student;` 的简写是（　）

A. `DES student;`  
B. `DESC student;`  
C. `SHOW student;`  
D. `STR student;`

80. 查看创建 `student` 表时所采用的完整定义，应使用（　）

A. `SHOW TABLE student;`  
B. `SHOW CREATE TABLE student;`  
C. `DESC CREATE student;`  
D. `SELECT CREATE TABLE student;`

81. 关于 `SHOW TABLES`、`DESC` 和 `SHOW CREATE TABLE`，下列说法正确的是（　）

A. 三者都用于删除表  
B. `SHOW TABLES` 查看有哪些表，`DESC` 查看基本结构  
C. `DESC` 用于选择当前数据库  
D. `SHOW CREATE TABLE` 只能查看表中的数据

---

## 六、修改表

82. 修改表结构的基本语句是（　）

A. `ALTER TABLE`  
B. `UPDATE TABLE`  
C. `MODIFY DATABASE`  
D. `CHANGE DATA`

83. 向 `student` 表中添加 `email` 列，正确的是（　）

A. `ALTER TABLE student ADD COLUMN email VARCHAR(100);`  
B. `ALTER TABLE student CREATE email VARCHAR(100);`  
C. `UPDATE TABLE student ADD email VARCHAR(100);`  
D. `CREATE COLUMN email IN student VARCHAR(100);`

84. 在 `student` 表第一列位置添加 `id` 列，可以使用（　）

A. `ALTER TABLE student ADD COLUMN id INT LAST;`  
B. `ALTER TABLE student ADD COLUMN id INT FIRST;`  
C. `ALTER TABLE student FIRST COLUMN id INT;`  
D. `ALTER TABLE student ADD FIRST id COLUMN INT;`

85. 在 `name` 列之后添加 `phone` 列，可以使用（　）

A. `ALTER TABLE student ADD phone VARCHAR(20) BEFORE name;`  
B. `ALTER TABLE student ADD phone VARCHAR(20) AFTER name;`  
C. `ALTER TABLE student ADD AFTER name phone VARCHAR(20);`  
D. `ALTER TABLE student APPEND phone TO name;`

86. 修改 `name` 列的数据类型和约束，但不修改列名，可以使用（　）

A. `ADD COLUMN`  
B. `MODIFY COLUMN`  
C. `DROP COLUMN`  
D. `SHOW COLUMN`

87. 将 `student` 表的 `name` 列修改为 `VARCHAR(50) NOT NULL`，正确的是（　）

A. `ALTER TABLE student MODIFY COLUMN name VARCHAR(50) NOT NULL;`  
B. `ALTER TABLE student UPDATE name VARCHAR(50) NOT NULL;`  
C. `MODIFY student COLUMN name VARCHAR(50) NOT NULL;`  
D. `UPDATE TABLE student MODIFY name VARCHAR(50);`

88. 使用 `CHANGE COLUMN` 修改列名时，通常还需要（　）

A. 重新写出该列的数据类型  
B. 删除整个数据库  
C. 指定存储引擎  
D. 重新创建所有表

89. 将 `name` 列改名为 `student_name`，并定义为 `VARCHAR(50)`，正确的是（　）

A. `ALTER TABLE student CHANGE COLUMN name student_name VARCHAR(50);`  
B. `ALTER TABLE student CHANGE name TO student_name;`  
C. `ALTER TABLE student MODIFY COLUMN name student_name VARCHAR(50);`  
D. `CHANGE TABLE student name student_name VARCHAR(50);`

90. 删除 `student` 表中的 `email` 列，正确的是（　）

A. `DROP COLUMN email FROM student;`  
B. `ALTER TABLE student DROP COLUMN email;`  
C. `DELETE COLUMN email FROM student;`  
D. `ALTER TABLE student DELETE email;`

91. 为 `student` 表添加主键，正确的是（　）

A. `ALTER TABLE student ADD PRIMARY KEY (student_id);`  
B. `ALTER TABLE student CREATE PRIMARY KEY student_id;`  
C. `UPDATE TABLE student ADD KEY student_id;`  
D. `ALTER TABLE student ADD FOREIGN KEY (student_id);`

92. 删除 `student` 表的主键，正确的是（　）

A. `ALTER TABLE student DELETE PRIMARY KEY;`  
B. `ALTER TABLE student DROP PRIMARY KEY;`  
C. `DROP PRIMARY KEY FROM student_id;`  
D. `ALTER TABLE student DROP KEY student_id PRIMARY;`

93. 为外键指定约束名 `fk_student_major`，正确的主要语法是（　）

A. `ADD CONSTRAINT fk_student_major FOREIGN KEY (...) REFERENCES ...`  
B. `ADD PRIMARY fk_student_major (...) REFERENCES ...`  
C. `ADD DEFAULT fk_student_major (...)`  
D. `ADD UNIQUE fk_student_major REFERENCES ...`

94. 删除名为 `fk_student_major` 的外键约束，正确的是（　）

A. `ALTER TABLE student DROP COLUMN fk_student_major;`  
B. `ALTER TABLE student DROP FOREIGN KEY fk_student_major;`  
C. `ALTER TABLE student DROP PRIMARY KEY fk_student_major;`  
D. `ALTER TABLE student DELETE FOREIGN KEY major_id;`

95. 删除外键时，通常需要指定（　）

A. 外键约束名称  
B. 被引用表中的任意数据值  
C. 数据库字符集  
D. 表中记录的行号

96. 将表 `student` 改名为 `students`，可以使用（　）

A. `ALTER TABLE student RENAME TO students;`  
B. `ALTER DATABASE student RENAME students;`  
C. `UPDATE TABLE student NAME students;`  
D. `CHANGE TABLE student INTO students;`

97. 下列也可以用于修改表名的是（　）

A. `RENAME TABLE student TO students;`  
B. `RENAME DATABASE student TO students;`  
C. `UPDATE TABLE student TO students;`  
D. `MODIFY TABLE student NAME students;`

98. 执行 `ALTER TABLE student DROP COLUMN email;` 后，会（　）

A. 只隐藏 `email` 列  
B. 删除 `email` 列及该列中的数据  
C. 删除整张 `student` 表  
D. 删除当前数据库

---

## 七、删除表与存储引擎

99. 删除 `student` 表的正确语句是（　）

A. `DELETE TABLE student;`  
B. `DROP TABLE student;`  
C. `REMOVE TABLE student;`  
D. `DROP DATABASE student;`

100. 为避免表不存在时报错，可以使用（　）

A. `DROP TABLE IF EXISTS student;`  
B. `DROP IF EXISTS TABLE student;`  
C. `DELETE TABLE IF EXISTS student;`  
D. `DROP TABLE IF NOT EXISTS student;`

101. 一次删除 `student` 和 `course` 两张表，正确的是（　）

A. `DROP TABLE student AND course;`  
B. `DROP TABLE student, course;`  
C. `DELETE TABLE student, course;`  
D. `DROP DATABASE student, course;`

102. `DROP TABLE student;` 与 `DELETE FROM student;` 的主要区别是（　）

A. 两者都会删除表结构  
B. 前者删除表结构和数据，后者主要删除数据并保留表结构  
C. 前者只删除数据，后者删除表结构  
D. 两者作用完全相同

103. 如果 `student` 表通过外键引用 `major` 表，删除两表时通常应先删除（　）

A. `major` 表  
B. `student` 表  
C. 当前数据库  
D. MySQL 系统数据库

104. 存储引擎主要决定（　）

A. 表中数据的存储和管理方式  
B. 数据库用户的姓名  
C. SQL 关键字的拼写  
D. 表中必须具有多少列

105. 查看 MySQL 支持的存储引擎，应使用（　）

A. `SHOW ENGINES;`  
B. `SHOW DATABASES;`  
C. `SHOW TABLES;`  
D. `DESC ENGINES;`

106. 现代 MySQL 通常默认使用的存储引擎是（　）

A. `InnoDB`  
B. `TEXT`  
C. `VARCHAR`  
D. `utf8mb4`

107. 下列属于 `InnoDB` 主要特点的是（　）

A. 不支持事务  
B. 不支持外键  
C. 支持事务和外键  
D. 只能保存字符数据

108. 将现有 `student` 表的存储引擎修改为 `InnoDB`，正确的是（　）

A. `ALTER TABLE student ENGINE = InnoDB;`  
B. `ALTER DATABASE student ENGINE = InnoDB;`  
C. `UPDATE TABLE student SET ENGINE = InnoDB;`  
D. `CHANGE ENGINE student TO InnoDB;`

---

## 八、综合应用题

109. 要建立教学数据库并在其中创建学生表，正确的语句顺序是（　）

A.

```
USE teaching;
CREATE DATABASE teaching;
CREATE TABLE student (id INT);
```

B.

```
CREATE DATABASE teaching;
USE teaching;
CREATE TABLE student (id INT);
```

C.

```
CREATE TABLE student (id INT);
CREATE DATABASE teaching;
USE teaching;
```

D.

```
DROP DATABASE teaching;
CREATE TABLE student (id INT);
USE teaching;
```

110. 下列学生表定义中，能够保证学号唯一且姓名不能为空的是（　）

A.

```
CREATE TABLE student (
    student_id CHAR(4),
    name VARCHAR(20)
);
```

B.

```
CREATE TABLE student (
    student_id CHAR(4) PRIMARY KEY,
    name VARCHAR(20) NOT NULL
);
```

C.

```
CREATE TABLE student (
    student_id CHAR(4) DEFAULT 'S001',
    name VARCHAR(20) UNIQUE
);
```

D.

```
CREATE TABLE student (
    student_id CHAR(4) NULL,
    name VARCHAR(20)
);
```

111. 创建商品表时，商品编号固定长度为 8，名称长度不固定，价格要求精确到小数点后 2 位，合理的数据类型组合是（　）

A. `INT、CHAR(1)、FLOAT`  
B. `CHAR(8)、VARCHAR(100)、DECIMAL(10,2)`  
C. `DATE、TEXT、VARCHAR(20)`  
D. `BIGINT、BLOB、TIME`

112. 下列选课表定义中，能够防止同一学生重复选修同一门课程的是（　）

A.

```
CREATE TABLE sc (
    student_id CHAR(4),
    course_id CHAR(4),
    score DECIMAL(5,2),
    PRIMARY KEY (student_id, course_id)
);
```

B.

```
CREATE TABLE sc (
    student_id CHAR(4),
    course_id CHAR(4),
    score DECIMAL(5,2),
    PRIMARY KEY (score)
);
```

C.

```
CREATE TABLE sc (
    student_id CHAR(4) PRIMARY KEY,
    course_id CHAR(4) PRIMARY KEY,
    score DECIMAL(5,2)
);
```

D.

```
CREATE TABLE sc (
    student_id CHAR(4),
    course_id CHAR(4),
    score DECIMAL(5,2)
);
```

113. 已有专业表：

```
CREATE TABLE major (
    major_id CHAR(4) PRIMARY KEY,
    major_name VARCHAR(50)
);
```

在学生表中建立对专业表的引用，正确的是（　）

A.

```
FOREIGN KEY (major_id)
REFERENCES major(major_id)
```

B.

```
PRIMARY KEY (major_id)
REFERENCES student(major_id)
```

C.

```
DEFAULT KEY (major_id)
REFERENCES major(major_name)
```

D.

```
CHECK KEY (major_id)
REFERENCES major
```

114. 依次查看数据库、选择 `school`、查看其中的表，正确的是（　）

A.

```
SHOW TABLES;
USE school;
SHOW DATABASES;
```

B.

```
SHOW DATABASES;
USE school;
SHOW TABLES;
```

C.

```
USE TABLES;
SHOW school;
DESC DATABASE;
```

D.

```
SHOW school;
CREATE DATABASE school;
DROP TABLES;
```

115. 要依次查看有哪些表、查看 `student` 表的基本结构、查看该表的完整创建定义，应执行（　）

A.

```
SHOW TABLES;
DESC student;
SHOW CREATE TABLE student;
```

B.

```
SHOW DATABASES;
USE student;
DROP TABLE student;
```

C.

```
DESC DATABASE student;
SHOW student;
CREATE TABLE student;
```

D.

```
SHOW CREATE DATABASE student;
SHOW TABLE student;
ALTER TABLE student;
```

116. 要依次给 `student` 表添加邮箱列、将邮箱列扩大到 200 个字符、再删除邮箱列，正确的是（　）

A.

```
ALTER TABLE student ADD COLUMN email VARCHAR(100);
ALTER TABLE student MODIFY COLUMN email VARCHAR(200);
ALTER TABLE student DROP COLUMN email;
```

B.

```
UPDATE student ADD email VARCHAR(100);
UPDATE student MODIFY email VARCHAR(200);
DELETE COLUMN email;
```

C.

```
CREATE TABLE student ADD email VARCHAR(100);
ALTER DATABASE student MODIFY email VARCHAR(200);
DROP TABLE email;
```

D.

```
ALTER TABLE student DROP COLUMN email;
ALTER TABLE student ADD COLUMN email VARCHAR(100);
DROP DATABASE email;
```

117. 下列关于 `NULL` 与空字符串的说法正确的是（　）

A. `NULL` 与空字符串 `''` 完全相同  
B. `NULL` 表示没有值或未知，空字符串是长度为 0 的字符串  
C. 空字符串表示未知，`NULL` 表示数字 0  
D. 二者都只能用于数值类型

118. 下列说法错误的是（　）

A. `SHOW DATABASES` 用于查看数据库  
B. `SHOW TABLES` 用于查看当前数据库中的表  
C. `DESC` 可以查看表的基本结构  
D. `USE` 用于删除数据库