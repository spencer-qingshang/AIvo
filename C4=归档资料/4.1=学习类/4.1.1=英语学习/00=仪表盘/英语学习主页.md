# 英语学习主页

## 🚀 当前活跃任务
*在这里显示来自 Conductor `plan.md` 的未完成任务。*

```dataview
TASK
FROM "conductor/tracks"
WHERE !completed
```

## 📚 近期学习素材
*列出最近在学习的素材。*

## 💡 待复习知识点
*根据掌握程度 (mastery) 和最后更新时间，列出最需要复习的 10 个知识点。*

```dataview
TABLE without id file.link as "知识点", mastery as "掌握度"
FROM "C4=归档资料/4.1=学习类/4.1.1=英语学习/20=知识库"
WHERE type = "vocab" OR type = "grammar"
SORT mastery ASC, file.mtime ASC
LIMIT 10
```

## 📈 学习统计
*统计本周新增单词等。*

```dataviewjs
const pages = dv.pages('"C4=归档资料/4.1=学习类/4.1.1=英语学习/20=知识库"').where(p => p.type == 'vocab');
const thisWeek = moment().subtract(7, 'days');
const newWordsThisWeek = pages.where(p => moment(p.file.ctime) > thisWeek).length;

dv.paragraph(`**本周新增单词**: ${newWordsThisWeek} 个`);
```
