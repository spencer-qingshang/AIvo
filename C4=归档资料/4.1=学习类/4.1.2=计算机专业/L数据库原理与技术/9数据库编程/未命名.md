# 第9章关系数据库设计理论选择题

以下均为单项选择题，每题只有一个正确答案。

### 1. 关系数据库是以（ ）为基本组织形式的数据库。

A. 层次结构  
B. 网状结构  
C. 关系  
D. 文件夹

### 2. 关系数据库设计理论主要研究（ ）。

A. 操作系统的进程管理  
B. 数据依赖、关系模式规范化和模式分解  
C. 网络通信协议  
D. 数据库备份技术

### 3. 判断一个关系模式是否“好”的重要依据是（ ）。

A. 表中数据是否很多  
B. 是否存在较多重复数据和更新异常  
C. 表名是否简短  
D. 字段是否全部为数值型

### 4. 关系模式规范化的主要目的是（ ）。

A. 增加数据冗余  
B. 减少数据冗余，避免插入、删除和修改异常  
C. 增加表中字段数量  
D. 提高计算机硬件性能

### 5. 下列不属于关系模式规范化主要目标的是（ ）。

A. 减少数据冗余  
B. 消除更新异常  
C. 保证数据依赖合理  
D. 尽可能把所有数据放在一张表中

### 6. 数据依赖描述的是关系中属性之间的（ ）。

A. 物理存储关系  
B. 逻辑约束关系  
C. 网络连接关系  
D. 用户权限关系

### 7. 最常见的数据依赖类型包括函数依赖和（ ）。

A. 多值依赖  
B. 连接依赖  
C. 包含依赖  
D. 主机依赖

### 8. 函数依赖 `X → Y` 的含义是（ ）。

A. X 和 Y 必须是同一个属性  
B. X 的值可以唯一确定 Y 的值  
C. Y 的值可以唯一确定 X 的值  
D. X、Y 都不能为 NULL

### 9. 在函数依赖 `X → Y` 中，X 称为（ ）。

A. 被决定属性  
B. 决定因素  
C. 非主属性  
D. 外码

### 10. 若关系中任意两个元组在属性 X 上的值相同，则它们在属性 Y 上的值也一定相同，可表示为（ ）。

A. `Y → X`  
B. `X → Y`  
C. `X ↔ Y`  
D. `X ≠ Y`

### 11. 设关系模式 `R(A,B,C)` 中存在 `A → B`，则 A 对 B 是（ ）。

A. 函数依赖  
B. 多值依赖  
C. 传递依赖  
D. 包含依赖

### 12. 如果 `X → Y`，但 X 的任何真子集都不能决定 Y，则称 Y 对 X 是（ ）。

A. 部分函数依赖  
B. 完全函数依赖  
C. 传递函数依赖  
D. 多值依赖

### 13. 设联合属性 `(A,B)` 是候选码，且 `A → C`，则 C 对 `(A,B)` 通常是（ ）。

A. 完全函数依赖  
B. 部分函数依赖  
C. 传递函数依赖  
D. 多值依赖

### 14. 如果 `X → Y`，且 X 的某个真子集也能决定 Y，则称 Y 对 X 是（ ）。

A. 完全函数依赖  
B. 部分函数依赖  
C. 传递函数依赖  
D. 平凡依赖

### 15. 若 `X → Y` 且 `Y → Z`，同时 X 不能直接决定 Z 的本质独立信息，则常称 Z 对 X 存在（ ）。

A. 部分函数依赖  
B. 完全函数依赖  
C. 传递函数依赖  
D. 多值依赖

### 16. 设 `学号 → 系号`，`系号 → 系主任`，则“学号 → 系主任”属于（ ）。

A. 部分函数依赖  
B. 传递函数依赖  
C. 多值依赖  
D. 平凡函数依赖

### 17. 能够唯一标识关系中每个元组的属性或属性组称为（ ）。

A. 外码  
B. 超码  
C. 候选码  
D. 非主属性

### 18. 能唯一标识元组，但不一定具有最小性的属性组称为（ ）。

A. 超码  
B. 候选码  
C. 主属性  
D. 外码

### 19. 候选码必须满足（ ）。

A. 唯一性和最小性  
B. 非唯一性和最小性  
C. 唯一性和最大性  
D. 允许被其他属性完全替代

### 20. 从候选码中选定的一个作为元组主要标识的码称为（ ）。

A. 外码  
B. 主码  
C. 超码  
D. 索引码

### 21. 候选码中的属性称为（ ）。

A. 主属性  
B. 非主属性  
C. 外部属性  
D. 冗余属性

### 22. 不包含在任何候选码中的属性称为（ ）。

A. 主属性  
B. 非主属性  
C. 决定属性  
D. 外码属性

### 23. 设关系模式 `R(A,B,C)`，函数依赖为 `A → B，B → C`，则 A 能够确定（ ）。

A. 只有 A  
B. A、B  
C. A、C  
D. A、B、C

### 24. 在上一题中，若 A 是唯一候选码，则 C 对 A 属于（ ）。

A. 部分函数依赖  
B. 传递函数依赖  
C. 完全函数依赖  
D. 多值依赖

### 25. 第一范式（1NF）要求关系中的属性值必须（ ）。

A. 可再分为多个值  
B. 是不可再分的原子值  
C. 全部为数字  
D. 全部唯一

### 26. 下列关系模式不满足 1NF 的典型情况是（ ）。

A. 一个字段中保存多个电话号码  
B. 每个字段保存一个整数  
C. 每行都有唯一编号  
D. 字段不允许为空

### 27. 第二范式（2NF）是在 1NF 基础上消除（ ）。

A. 传递函数依赖  
B. 部分函数依赖  
C. 多值依赖  
D. 所有函数依赖

### 28. 一个关系模式满足 2NF，必须首先满足（ ）。

A. 1NF  
B. 3NF  
C. BCNF  
D. 4NF

### 29. 若候选码只有一个属性，则非主属性对该候选码（ ）。

A. 一定存在部分函数依赖  
B. 不可能存在部分函数依赖  
C. 一定存在传递依赖  
D. 一定存在多值依赖

### 30. 设关系 `选课(学号, 课程号, 学生姓名, 课程名, 成绩)`，候选码为 `(学号, 课程号)`，且 `学号 → 学生姓名`，则学生姓名对候选码属于（ ）。

A. 完全函数依赖  
B. 部分函数依赖  
C. 传递函数依赖  
D. 多值依赖

### 31. 将含有部分函数依赖的关系分解，通常可以（ ）。

A. 消除部分函数依赖  
B. 增加部分函数依赖  
C. 消除所有数据依赖  
D. 使所有属性都成为主属性

### 32. 第三范式（3NF）是在 2NF 基础上主要消除（ ）。

A. 原子性  
B. 部分函数依赖  
C. 传递函数依赖  
D. 多值依赖

### 33. 一个关系模式满足 3NF，必须首先满足（ ）。

A. 1NF  
B. 2NF  
C. BCNF  
D. 4NF

### 34. 3NF 的判断条件之一是：对于非平凡函数依赖 `X → A`，以下说法正确的是（ ）。

A. X 必须是普通属性  
B. X 是超码，或者 A 是主属性  
C. A 必须是非主属性  
D. X 和 A 必须都是候选码

### 35. 下列哪种情况违反 3NF？

A. 决定因素是超码  
B. 被决定属性是主属性  
C. 决定因素不是超码，且被决定属性不是主属性  
D. 依赖是平凡函数依赖

### 36. 设关系 `学生(学号, 系号, 系主任)`，函数依赖为 `学号 → 系号`、`系号 → 系主任`，若学号是候选码，则该关系主要违反（ ）。

A. 1NF  
B. 2NF  
C. 3NF  
D. 4NF

### 37. BCNF 比 3NF 的要求（ ）。

A. 更宽松  
B. 更严格  
C. 完全相同  
D. 只针对多值依赖

### 38. BCNF 要求对于每一个非平凡函数依赖 `X → Y`，X 必须是（ ）。

A. 非主属性  
B. 主属性  
C. 超码  
D. 外码

### 39. 下列关于 BCNF 的说法正确的是（ ）。

A. 满足 3NF 的关系一定满足 BCNF  
B. 满足 BCNF 的关系一定满足 3NF  
C. BCNF 只要求属性值原子化  
D. BCNF 只处理多值依赖

### 40. 一个关系模式可能满足 3NF 但不满足 BCNF，说明（ ）。

A. 3NF 的要求比 BCNF 严格  
B. BCNF 的要求比 3NF 严格  
C. 两者没有区别  
D. 该关系一定不满足 1NF

### 41. 多值依赖通常表示（ ）。

A. 一个属性值决定另一个唯一属性值  
B. 一个属性值对应一组相互独立的属性值  
C. 一个表引用另一个表  
D. 一个属性值不能重复

### 42. 多值依赖通常表示为（ ）。

A. `X → Y`  
B. `X →→ Y`  
C. `X == Y`  
D. `X ⇔ Y`

### 43. 在 `R(X,Y,Z)` 中，若给定 X 后，Y 和 Z 相互独立，则可以表示为（ ）。

A. `X → Y`  
B. `Y → X`  
C. `X →→ Y`  
D. `Y → Z`

### 44. 第四范式（4NF）主要用于消除（ ）。

A. 部分函数依赖  
B. 传递函数依赖  
C. 非平凡多值依赖  
D. 主键依赖

### 45. 4NF 要求对于每一个非平凡多值依赖 `X →→ Y`，X 必须是（ ）。

A. 超码  
B. 非主属性  
C. 外码  
D. 普通属性

### 46. 如果一个关系中存在两个相互独立的多值事实，通常应当（ ）。

A. 合并为一个属性  
B. 分解关系模式  
C. 删除其中一个事实  
D. 增加重复数据

### 47. 关系模式分解的主要目的不包括（ ）。

A. 减少数据冗余  
B. 消除更新异常  
C. 保持合理的数据依赖  
D. 任意增加关系数量

### 48. 无损连接分解是指分解后的关系重新连接时（ ）。

A. 可能产生额外元组  
B. 能够还原原关系，不产生信息损失  
C. 必然丢失部分数据  
D. 只保留主码

### 49. 依赖保持分解是指（ ）。

A. 分解后所有表都没有依赖  
B. 分解后的关系仍能保持原有函数依赖的约束  
C. 只保留主键依赖  
D. 不需要任何约束

### 50. 一个关系模式规范化的通常过程是（ ）。

A. 4NF → BCNF → 3NF → 2NF → 1NF  
B. 1NF → 2NF → 3NF，并可继续到 BCNF、4NF  
C. 3NF → 1NF → 4NF  
D. 2NF → 1NF → BCNF

### 51. Armstrong 公理系统用于（ ）。

A. 计算机网络寻址  
B. 推理函数依赖  
C. 设置数据库密码  
D. 生成随机数据

### 52. Armstrong 公理系统中的自反律是（ ）。

A. 若 `Y ⊆ X`，则 `X → Y`  
B. 若 `X → Y`，则 `Y → X`  
C. 若 `X → Y` 且 `Y → Z`，则 `X → Z`  
D. 若 `X → Y`，则 `X ∪ Z → Y ∪ Z`

### 53. Armstrong 公理系统中的增广律是（ ）。

A. 若 `Y ⊆ X`，则 `X → Y`  
B. 若 `X → Y`，则 `XZ → YZ`  
C. 若 `X → Y` 且 `Y → Z`，则 `X → Z`  
D. 若 `X → Y`，则 `Y → X`

### 54. Armstrong 公理系统中的传递律是（ ）。

A. 若 `Y ⊆ X`，则 `X → Y`  
B. 若 `X → Y`，则 `XZ → YZ`  
C. 若 `X → Y` 且 `Y → Z`，则 `X → Z`  
D. 若 `X → Z`，则 `X → Y`

### 55. 由 Armstrong 三条基本公理可以推导出的合并律是（ ）。

A. 若 `X → Y` 且 `X → Z`，则 `X → YZ`  
B. 若 `X → Y`，则 `Y → X`  
C. 若 `X → Y` 且 `Y → Z`，则 `Z → X`  
D. 若 `X → Y`，则 `X → ∅`

### 56. 由 Armstrong 公理可知，若 `X → Y` 且 `Y → Z`，则（ ）。

A. `X → Z`  
B. `Z → X`  
C. `Y → XZ`  
D. `XZ → Y`

### 57. 属性集 X 关于函数依赖集 F 的闭包记为（ ）。

A. `F+`  
B. `X+`  
C. `R+`  
D. `X-F`

### 58. 属性集 X 的闭包 `X+` 表示（ ）。

A. 能被 X 函数确定的全部属性集合  
B. X 的补集  
C. 所有候选码的集合  
D. 所有非主属性的集合

### 59. 计算属性闭包的初始步骤通常是（ ）。

A. 令 `X+ = ∅`  
B. 令 `X+ = X`  
C. 令 `X+ = R`  
D. 令 `X+ = F`

### 60. 计算闭包时，如果函数依赖 `Y → Z` 的左部 Y 已包含在当前闭包中，则应当（ ）。

A. 删除 Y  
B. 将 Z 加入闭包  
C. 删除 Z  
D. 停止计算并判定错误

### 61. 设 `R(A,B,C)`，F = `{A → B, B → C}`，则 `A+` 为（ ）。

A. `{A}`  
B. `{A,B}`  
C. `{A,C}`  
D. `{A,B,C}`

### 62. 设 `R(A,B,C,D)`，F = `{A → B, B → C, C → D}`，则 A 是（ ）。

A. 候选码  
B. 非主属性  
C. 超码但不是候选码  
D. 外码

### 63. 在上一题中，A 的闭包是（ ）。

A. `{A}`  
B. `{A,B}`  
C. `{A,B,C}`  
D. `{A,B,C,D}`

### 64. 设 `R(A,B,C)`，F = `{A → B, B → A}`，则候选码可能是（ ）。

A. A 和 B  
B. 只有 A  
C. 只有 B  
D. A、B、C

### 65. 设 `R(A,B,C)`，F = `{AB → C}`，则候选码是（ ）。

A. A  
B. B  
C. AB  
D. ABC

### 66. 确定候选码时，首先应寻找（ ）。

A. 闭包不能扩大的属性  
B. 能够函数确定全部属性的最小属性集  
C. 所有非主属性  
D. 任意一个属性

### 67. 如果某属性从未出现在函数依赖右部，通常它（ ）。

A. 一定不是候选码的一部分  
B. 通常必须包含在候选码中  
C. 一定是非主属性  
D. 一定是外码

### 68. 若一个属性集的闭包等于关系模式的全部属性，则该属性集是（ ）。

A. 一个超码  
B. 一定是最小超码  
C. 一定是非主属性集  
D. 一定不是候选码

### 69. 要判断一个超码是否为候选码，还需要检查其（ ）。

A. 是否包含所有属性  
B. 是否具有最小性  
C. 是否包含外码  
D. 是否包含 NULL

### 70. 下列说法正确的是（ ）。

A. 所有超码都是候选码  
B. 所有候选码都是超码  
C. 候选码不能唯一标识元组  
D. 非主属性一定能组成候选码

---

# 参考答案

1. C
2. B
3. B
4. B
5. D
6. B
7. A
8. B
9. B
10. B
11. A
12. B
13. B
14. B
15. C
16. B
17. C
18. A
19. A
20. B
21. A
22. B
23. D
24. B
25. B
26. A
27. B
28. A
29. B
30. B
31. A
32. C
33. B
34. B
35. C
36. C
37. B
38. C
39. B
40. B
41. B
42. B
43. C
44. C
45. A
46. B
47. D
48. B
49. B
50. B
51. B
52. A
53. B
54. C
55. A
56. A
57. B
58. A
59. B
60. B
61. D
62. A
63. D
64. A
65. C
66. B
67. B
68. A
69. B
70. B